# Архитектура проекта "Матюня"

## 1. Ключевые Принципы ("Конституция")

Проект "Матюня" построен на нескольких ключевых принципах, выработанных в ходе разработки и рефакторинга. Эти принципы являются основой для всех будущих модулей.

### 1.1. Данные отдельно, Код отдельно

Это наш самый главный закон. Мы никогда не смешиваем "сырые" данные (тексты, числа, параметры) с логикой их обработки.

-   **Данные** лежат в папке `data/`. Это `.json`-файлы, которые может редактировать даже не-программист.
-   **Код** лежит в папках `handlers/`, `task_generators/`, `utils/` и т.д. Он читает данные и работает с ними, но никогда не содержит их внутри себя.

**Преимущества:** Гибкость, масштабируемость, простота обновления контента.

### 1.2. "Движок + Плагины"

Мы не пишем одного "бота-монстра". Мы строим универсальный "движок" и подключаем к нему независимые "плагины" (модули).

-   **Движок:** Это универсальные системы, такие как `MessageManager` (`utils/message_manager.py`), система FSM состояний (`states/`), `db_manager` (`utils/db_manager.py`).
-   **Плагины:** Это папки для конкретных групп заданий (e.g., `task_generators/tasks_1_5/`), которые содержат всю специфичную для них логику (`calculator.py`, `config.py`) и данные.

**Преимущества:** Изоляция. Работа над модулем "Шины" не может сломать "Задание 12".

### 1.3. "Тупой, но надежный" Конструктор

Наши генераторы (`TaskGenerator`) не должны быть слишком "умными". Их задача — работать как конвейер: взять "чертеж" (`plot.json`), взять "детали" (`texts.json`), вызвать "специалистов" (`calculator`, `renderer`) и собрать готовый продукт (`task_package`). Вся "думалка" вынесена в модули-"специалисты".

**Преимущества:** Надежность, предсказуемость, простота отладки.

---

## 2. Структура Папок (Верхний уровень)

-   **/core/**: "Ядро" проекта. Содержит базовые, низкоуровневые сущности, от которых зависит вся система (например, определения `CallbackData`).
-   **/data/**: Все статические данные проекта (тексты, числовые "плоты", изображения).
-   **/docs/**: Документация проекта (Markdown-файлы).
-   **/gpt/**: Все, что связано с GPT (промпты, пулы фраз, утилиты для работы с API).
-   **/handlers/**: Модули, которые непосредственно реагируют на действия пользователя в Телеграме. **"Слой Представления" (View Layer)**.
-   **/help_core/**: Ядро системы Помощи. Изолированный модуль, содержащий "Решателей", "Оживителей" и т.д.
-   **/keyboards/**: "Сборщики" Telegram-клавиатур. Отвечают за визуальное представление кнопок.
-   **/py_generators/**: (Устаревшее) Старые, процедурные генераторы. **Подлежат замене.**
-   **/scripts/**: Служебные скрипты для разработчика (миграции, генераторы данных, тесты). Не являются частью работающего бота.
-   **/states/**: Определения FSM-состояний.
-   **/task_generators/**: "Сердце" проекта. **"Бизнес-логика" (Business Logic Layer)**. Код, отвечающий за сборку заданий.
-   **/tests/**: Автоматические и ручные тесты. Структура папок в `tests/` зеркально повторяет структуру основного проекта.
-   **/utils/**: Вспомогательные утилиты, которые используются по всему проекту (`db_manager`, `message_manager`, `answer_utils`).

---

## 3. Ключевые Сущности (Объекты данных)

### `task_package`
Это главный объект, который "путешествует" по системе. `TaskGenerator` создает его, а хендлеры используют для отображения.

-   **Назначение:** Содержит **всю** информацию, необходимую для проведения одного полного сеанса решения задачи (например, 5 заданий про "Шины").
-   **Ключевые поля:**
    -   `subtype`: Подтип задания (e.g., `"tires"`).
    -   `display_scenario`: "Сценарий" для показа обзорного экрана (картинки, тексты).
    -   `tasks`: Список из 5 объектов, где каждый — это одно задание с текстом, ответом и HTML-таблицей.
    -   `db_task_ids`: Список ID, под которыми эти 5 задач зарегистрированы в БД.
    -   `plot_data`: Полная копия `plot.json`, использованного для генерации. **Важно для системы Помощи.**

### `state` (FSM Context)
"Оперативная память" бота о текущем пользователе.

-   **Назначение:** Хранит данные, актуальные для текущей сессии пользователя.
-   **Ключевые поля:**
    -   `student_name`, `gender`: Данные о пользователе.
    -   `message_log` / `tracked_messages`: "Журнал" отправленных сообщений для `MessageManager`.
    -   `task_package`: Копия `task_package` для текущей сессии.
    -   `task_ids`: Копия `db_task_ids`.
    -   `current_task_index`: Номер задания, которое решается прямо сейчас (0-4).
    -   `solved_tasks_indices`: Список индексов уже решенных заданий.
    -   `current_overview_tag`: Уникальный тег "пульта управления".


    # Обзор Проекта "Матюня"

## 1. Концепция

**Матюня** — это интеллектуальный Telegram-бот, разработанный для помощи подросткам в подготовке к ОГЭ по математике.

-   **Целевая аудитория:** Ученики 8-9 классов.
-   **Ключевая идея:** Создать не просто "решатель", а **"честного и умного помощника"**, который мотивирует, объясняет сложные темы простым языком и помогает выработать уверенность в своих силах.
-   **Стратегия:** Проект построен на **"Гибридном Интеллекте"**, где мощь больших языковых моделей (LLM) используется на этапе **офлайн-подготовки контента** под контролем человека, а сам бот в "боевом режиме" работает на быстрой, надежной и предсказуемой логике без онлайн-обращений к GPT для генерации задач.

---

## 2. Технический Стек

Проект построен на современном и асинхронном стеке Python.

-   **Язык:** Python 3.11+
-   **Telegram Bot Framework:** `aiogram` 3.x
-   **База Данных:** `SQLite` (через `SQLAlchemy` 2.x Core & ORM)
    -   **Драйвер:** `aiosqlite` (для асинхронной работы)
-   **Работа с LLM (GPT):** Библиотека `openai`
-   **Генерация Изображений:** `Pillow` (PIL)
-   **Конфигурация:** `.env` файлы (через `python-dotenv`)
-   **Зависимости:** Управляются через `requirements.txt`.

---

## 3. Ключевые "Фичи" и Реализации

### 3.1. "Идеальный Завод 2.0" (Модуль Заданий 1-5)
Продвинутая система генерации практико-ориентированных задач, основанная на принципе **"Данные отдельно, Код отдельно"**. Позволяет легко добавлять новые подтипы (Шины, Печи), не изменяя основной код генератора.

### 3.2. "Живой Рабочий Лист"
Интерактивный UX/UI для решения задач 1-5. Пользователь не переключается между экранами, а **накапливает** решенные задания в одном "полотне", получая мгновенную обратную связь и видя свой прогресс в реальном времени.

### 3.3. "Консилиум Экспертов" (Система Помощи)
Модульная система помощи, состоящая из трех независимых компонентов:
-   **"Диспетчер" (`handler`):** Принимает запрос и маршрутизирует его.
-   **"Решатель" (`solver`):** Чистый математический модуль, который производит вычисления и возвращает структурированный "отчет".
-   **"Оживитель" (`humanizer`):** GPT-модуль, который превращает "отчет" "Решателя" в дружелюбное, пошаговое объяснение.

### 3.4. "Идеальная Чистота" (`MessageManager`)
Продвинутая система управления сообщениями в чате. Каждое отправленное сообщение регистрируется с **категорией** и **тегом**, что позволяет проводить "хирургическую" очистку интерфейса (удалять конкретные панели, временные уведомления и т.д.), избегая "мерцания" и хаоса.


🐾 Матюня — не просто бот. Это первый, кто скажет: «Да, ты на правильном пути. Да, ошибка — это не конец. Да, ты уже почти понял. Давай ещё раз вместе».
