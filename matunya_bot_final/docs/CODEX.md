> После сохранения `AGENTS.md` перезапускать VS Code не нужно — Codex увидит файл сам.

# 3) Клавиши для скорости (опционально)
- **Codex (focus view):** назначь, например, `Ctrl+Alt+C`.  
  Settings → Keyboard Shortcuts → `Codex: Focus on Codex View` → двойной клик в *Keybinding* → нажми `Ctrl+Alt+C`.
- **Explorer:** `Ctrl+Shift+E`.
- **Показать/скрыть боковую:** `Ctrl+B`.

# 4) Готовые фразы, которые «понимает» Codex
Скопируй и используй как шаблоны (он будет учитывать `AGENTS.md`):

- **Исправь баг (с тестом):**  
  `Fix the bug in handlers/.../help_1_5_handler.py: start_gpt_dialog_handler must accept Bot directly. Keep aiogram v3 style. Add a pytest reproducer. Show diff.`

- **Рефакторинг без изменения поведения:**  
  `Refactor utils/db_manager.py:log_ai_interaction to reduce duplication and add type hints. Keep behavior identical. If needed, add small unit tests. Show diff.`

- **Наведи порядок:**  
  `Run ruff --fix and black on changed files, then mypy on utils/. Report violations before applying.`

- **Сгенерируй тесты к функции:**  
  `Create pytest tests for utils/db_manager.py::log_ai_interaction. Cover success and error paths. No I/O or network.`

# 5) Когда включать Cloud
- Большие рефакторинги, миграции, генерация пачки тестов/фиксов.  
- В Codex-панели снизу переключи **Local → Cloud** перед запуском такой задачи.

Пиши ответ по-русски.
Сначала покажи diff, ничего не записывай, жди моей команды "Apply".

В файле utils/db_manager.py в функции log_ai_interaction:
— добавь точные type hints и короткий docstring в стиле Google,
— не меняй поведение,
— дай краткое резюме после diff.

Пиши ответ по-русски.
Сначала diff, без записи.

В handlers/.../help_1_5_handler.py отредактируй start_gpt_dialog_handler:
— функция должна принимать Bot напрямую,
— заменить обращения callback.bot на параметр bot,
— ничего лишнего не трогать.

Выполни локально. Учитывай правила из AGENTS.md. Пиши по-русски. Сначала покажи diff, ничего не записывай — жди моей команды "Apply".

Задача: Создать "скелет" класса Task12Generator с методами загрузки данных.

Только посмотреть изменения:
Сначала покажи diff, ничего не записывай — жди моей команды "Apply".

Безопасность:
Учитывай правила из AGENTS.md. Не читать .env и секреты, использовать моки.

Локально/в облаке:
Выполни локально / Переключись в Cloud-режим и выполни там.

Рефакторинг без изменения поведения:
Отрефакторь X, добавь type hints и краткий docstring, поведение не менять.

Тесты:
Сгенерируй unittest/pytest для функции Y; секреты — через os.environ, без .env.

Короткое резюме:
После diff дай краткое резюме изменений (3–4 пункта).

Пиши по-русски. Сначала покажи diff, ничего не записывай — жди моей команды "Apply".
(дальше — что именно нужно изменить)


Turn TODOs into Codex tasks Write a TODO comment and convert it into a Codex task with a single click.
👉 “Turn TODOs into Codex tasks” значит:
Ты пишешь в коде комментарий с # TODO ..., а потом можешь одним кликом превратить его в задачу для Codex.

🔹 Пример:
def calculate_discount(price, percent):
    # TODO: handle cases when percent > 100
    return price * (1 - percent / 100)


Ты оставляешь комментарий с # TODO.

Наводишь мышкой → появится кнопка “Send to Codex”.

Codex поймёт, что ты хочешь сделать, и предложит готовое решение:

добавить проверку

вернуть исключение или исправленную логику

🔹 Зачем это нужно:

Не держать все мысли в голове.

Записал “TODO” → Codex потом сам его решит.

Работает как персональный помощник, который дочитывает твои заметки и превращает их в рабочий код.


Hand off to Codex in the cloud Send tasks to Codex to run in the background so you can stay focused and move faster.
👉 “Hand off to Codex in the cloud” означает:
Ты можешь отправить задачу (например, “сделай рефакторинг проекта” или “сгенерируй тесты для этих функций”) не локально в VS Code, а на сервера OpenAI.

Тогда:

Codex выполняет задачу в фоне (как бы отдельно от твоего редактора).

Ты можешь продолжать работать — не нужно ждать в VS Code.

Когда результат готов, он вернётся прямо в редактор.

🔹 Когда полезно:

Если задача большая и тяжёлая (например, рефакторинг сотен строк).

Если ты хочешь, чтобы Codex сам “додумал” и довёл задачу до конца без твоего вмешательства.

Если нужно несколько часов вычислений (новый GPT-5-Codex может работать до 7 часов подряд).

⚡ То есть это что-то вроде “отправить задачу ассистенту и не отвлекаться”.
Он будет работать за тебя в облаке, а ты можешь параллельно делать другое.

Decide how much autonomy you want to grant For more details, see the Codex docs Codex can make mistakes Review the code it writes and commands it runs Powered by your ChatGPT account Uses your plan’s rate limits and training data preferences
🔹 Что это значит:

Decide how much autonomy you want to grant
– Ты сама выбираешь, насколько много свободы дать Codex:

только писать куски кода,

или ещё и запускать команды (например, тесты, миграции, сборку).

Codex can make mistakes
– Codex умный, но не идеальный. Он может:

предложить нерабочий код,

сделать логическую ошибку,

или затереть что-то важное.
Поэтому всегда проверяй, что он пишет.

Review the code it writes and commands it runs
– Обязательно просматривай его изменения, прежде чем принимать их.
(Codex всегда показывает diff / подсказку, прежде чем реально применить изменения.)

Powered by your ChatGPT account
– Он работает через твой OpenAI аккаунт (тот же, что у тебя для ChatGPT Plus).

Uses your plan’s rate limits and training data preferences
– Codex ограничен лимитами твоего тарифа (у тебя ChatGPT Plus).
– Также учитываются твои настройки: участвует ли твой код в тренировке моделей или нет.

⚡ По сути, это “правила безопасности”:
Codex — мощный помощник, но ты всегда остаёшься главным редактором.
