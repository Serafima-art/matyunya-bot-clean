# Документация: Система Помощи ("Консилиум Экспертов")

## 1. Концепция

Система Помощи в "Матюне" построена на принципе **разделения ответственности**. Вместо одного монолитного модуля, который пытается и решать, и объяснять, мы используем цепочку из трех независимых "специалистов".

Это позволяет добиться максимальной надежности в расчетах и максимального качества в текстовых объяснениях.

## 2. Архитектура "Консилиума"

Система состоит из трех ключевых компонентов, которые живут в папке `help_core/` и вызываются специальным хендлером.

### 2.1. "Диспетчер Помощи" (`help_1_5_handler.py`)

-   **Роль:** "Главный врач", который принимает запрос на помощь.
-   **Где живет:** `handlers/callbacks/task_handlers/group_1_5/`
-   **Логика работы:**
    1.  "Ловит" нажатие на кнопку `[💡 Помощь]`, используя `TaskCallback`.
    2.  Из `callback_data` извлекает номер вопроса (`question_num`).
    3.  Из `state` достает `task_package`.
    4.  **Готовит "историю болезни":** На основе `question_num` и `task_package` он собирает все необходимые **числовые данные** из `plot_data`.
    5.  **Вызывает "узкого специалиста":** Передает эти данные соответствующему "Решателю" (например, `solve_q1_tires`).
    6.  Получив от "Решателя" структурированный JSON-отчет (`solution_core`), передает его "Оживителю".
    7.  Получив от "Оживителя" готовый HTML-текст, отправляет его пользователю.

### 2.2. "Решатель" (`.../solvers/task_1_5_solvers.py`)

-   **Роль:** "Хирург" или "Математик". Узкий специалист, который знает **только математику** своего задания.
-   **Где живет:** `help_core/solvers/`
-   **Принципы работы:**
    -   **Никакого GPT.** Только чистый, детерминированный Python-код.
    -   **Никакого `aiogram`.** Не знает ничего о Телеграме, чатах или сообщениях.
    -   **Принимает на вход:** Простой словарь с числовыми данными.
    -   **Возвращает:** Стандартизированный JSON-объект (`solution_core`).

### 2.3. "Оживитель" (`.../humanizers/solution_humanizer.py`)

-   **Роль:** "Переводчик" или "Психотерапевт". Превращает сухие данные в "живой" текст.
-   **Где живет:** `help_core/humanizers/`
-   **Логика работы:**
    1.  Получает на вход JSON-объект `solution_core` от "Решателя".
    2.  "Упаковывает" этот JSON в большой системный промпт.
    3.  Вызывает GPT (`ask_gpt_with_history`), передавая ему промпт с инструкциями по стилю и форматированию.
    4.  Возвращает готовый, красивый HTML-текст, который "Диспетчер" отправит пользователю.

---

## 3. "Стандарт Отчета" (`solution_core`)

Это единый формат данных, в котором **все "Решатели"** обязаны возвращать результат. Это позволяет "Оживителю" быть универсальным.

```json
{
    "question_id": "tires_q1_01",
    "explanation_idea": "Чтобы найти наименьшую ширину шины для диска, нам нужно посмотреть в таблицу и выбрать самое маленькое значение из доступных.",
    "calculation_steps": [
        {
            "step_number": 1,
            "description": "Находим в таблице все размеры для диска 16 дюймов.",
            "calculation_result": "185/65, 195/60, 205/55, 205/60, 215/55"
        },
        {
            "step_number": 2,
            "description": "Извлекаем из них ширину (первое число).",
            "calculation_result": "185, 195, 205, 215"
        },
        {
            "step_number": 3,
            "description": "Находим наименьшее из этих чисел.",
            "calculation_result": "185 мм"
        }
    ],
    "final_answer": {
        "value_machine": 185,
        "value_display": "185 мм",
        "unit": "мм"
    },
    "hints": [
        "Ширина шины — это всегда первое число в маркировке.",
        "Не перепутай ширину шины и диаметр диска."
    ]
}
```
Этот стандарт является **ключом** к работе всей системы Помощи.




Структура после глобального рефакторинга Помощи. Сейчас Помощ разделена на две категориии 1. Решатель-Оживитель-ГПТ (задание 1-5), 2. Решатель-Оживитель (задание 11)

handlers/

callbacks/
dialogs/
gpt_dialog_control_handler.py — общий центр управления гибридным диалогом: следит за состоянием GPState.in_dialog, принимает вопросы ученика, формирует запросы к GPT с накопленной историей, отправляет ответы и утилизирует переписку по кнопкам ❓ Ещё вопрос / ✅ Всё понятно.
task_handlers/
group_1_5/
help_handler.py — фронтовой хендлер помощи для задач 1‑5: подготавливает и отправляет структурированную подсказку, очищает лишние клавиатуры, запускает диалог (запоминает solution_core и промпт, переводит FSM в GPState.in_dialog и отправляет первое приглашение).
task_11/
help_handler.py — аналогичный хендлер для задания 11: получает HTML-подсказку от диспетчера, обнуляет клавиатуры, записывает промпт/контекст и передаёт управление общему диалоговому контроллеру; отличается тем, что работает с solution_core нового формата и персонализированным промптом.
message_handlers/
group_1_5_answer_handler.py — обрабатывает текстовые ответы учеников для задач 1‑5: проверяет корректность, подсвечивает ошибки, обновляет UI после каждой попытки.
task_11_answer_handler.py — отвечает за проверку многошаговых ответов по заданию 11: сверяет по токенам, обновляет строку «Ответ: …» в основном сообщении, переключает клавиатуры по статусу (успех/повтор).
help_core

dispatchers/
task_1_5/help_handler_1_5.py — «специалист»‑диспетчер, который в конвейере помощи «подбирает решателя», запускает humanizer и готовит данные для фронтового хендлера (HTML‑текст + solution_core).
task_11/help_handler_11.py — похожий диспетчер для задания 11, но в «гибридном» стандарте: динамически импортирует нужный solver, нормализует solution_core, передаёт его humanizer’у и возвращает готовый HTML; ключевое отличие — строгий формат solution_core и связка с task_11_dialog_prompts.
prompts/
dialog_prompts.py — универсальный генератор системных промптов для задач 1‑5: учитывает имя/пол, историю и solution_core, выдаёт «дружелюбного Матюню» с HTML‑инструкциями.
task_11_dialog_prompts.py — специализированный промпт для задания 11: коротко напоминает структуру решения, учитывает имя/пол, запрещает повтор полного разбора.
solvers/ — решатели приведены к единому solution_core‑формату (question_id, explanation_idea, calculation_steps, final_answer, hints); каждая задача/подтип имеет модуль с функцией solve(task_package) и возвращает нормализованный словарь, который далее используют диспетчеры и humanizer.
keyboards

navigation/
help_dialog_navigation.py — центральное место для клавиатур помощи: содержит get_help_panel_keyboard (кнопки «Скрыть» / «Задать вопрос») и get_gpt_dialog_keyboard (кнопки завершения/продолжения диалога); решает проблему дублирования UI‑логики и гарантирует синхронное изменение клавиатур во всех сценариях помощи.
Этот обзор отражает текущую архитектуру: диспетчеры готовят контент, task‑хендлеры управляют UI и состояниями, gpt_dialog_control_handler ведёт диалог, а общие утилиты (prompts, solvers, keyboards) обеспечивают единый стиль и поведение.
