# Технический Журнал Проекта "Матюня"

Этот документ содержит список задач, улучшений и архитектурных решений, которые нужно реализовать или проверить в будущем.

## ❗️ Задачи с Высоким Приоритетом

❗️❗️❗️"Внедрение Контроля Качества": Мы улучшаем наш скрипт scripts/generate_tire_plots.py. Мы добавим в него вызов этого "Валидатора". После генерации каждого нового плота, скрипт будет сразу же отдавать его на проверку. Если "Валидатор" найдет ошибку, скрипт сообщит нам об этом и остановится.

- [ ] **Протестировать `utils/answer_utils.py`:**
  - **Что сделано:** Мы провели "слияние" двух файлов (`answer_utils.py` и `answer_check.py`), создав универсального "Судью".
  - **Что нужно проверить:** Необходимо написать тесты в папке `tests/utils/`, чтобы убедиться, что новый "Судья" корректно обрабатывает ВСЕ типы ответов:
    - Простые числа (`185`).
    - Десятичные дроби (`-1.7`, `-1,7`).
    - Ответы со словами (`уменьшится на 3.8`).
    - Обыкновенные дроби (`1/2`).
    - Смешанные дроби (`1 1/2`).
    - Ответы с корнями (`2 √ 3`).

- [ ] **Провести ревизию `utils/text_formatters.py`:**
  - **Что сделано:** Мы определили этот файл как полезный, но частично устаревший.
  - **Что нужно сделать:** Когда будем работать над Заданиями 6, 7 и т.д., нужно проверить, используется ли функция `format_task`. Если да — убедиться, что она работает правильно. Если нет — удалить ее. Начать активно использовать остальные форматеры (`format_success`, `format_warning`) по всему проекту для унификации стиля сообщений.

  для Задания 7 нужно будет создать свой ..._router.py, config.py и т.д., и что image_generator.py является его частью.

  - [ ] **Провести рефакторинг системы Помощи:**
  - **Проблема:** Логика "сброса состояния" (`utils/help_reset.py`) вынесена в отдельную утилиту.
  - **Что нужно сделать:** Перенести функцию `reset_help_state` внутрь основного "Диспетчера Помощи" (вероятно, `help_1_5_handler.py` или будущего `help_dispatcher.py`). Модуль Помощи должен сам отвечать за управление своим состоянием. После переноса — удалить `utils/help_reset.py`.

  - [ ] **Провести рефакторинг "старых" заданий (6, 7, 8 и т.д.):**
  - **Проблема:** Логика для этих заданий использует устаревшую архитектуру.
  - **Компоненты "старой" архитектуры:**
    - `handlers/_legacy/task_utils.py` (старый "диспетчер")
    - `handlers/_legacy/task_loader.py`, `handlers/_legacy/task_repo.py` (старые "менеджеры БД")
    - `py_generators/` (вся папка со старыми генераторами)
  - **Что нужно сделать:** При переходе к разработке Задания 6 (или 7, 8...), необходимо **полностью удалить** соответствующий старый генератор (e.g., `py_generators/task_6_generator.py`) и все связанные с ним утилиты из `_legacy/`. Вместо них создать новую структуру по образцу `task_generators/tasks_1_5/` с `generator.py`, `calculator.py` и `config.py`.
  - **Обновить "Принимающих" (хендлеры):** Клавиатуры в `gpt_parts_keyboard.py` были обновлены и теперь используют `TaskCallback`. Необходимо будет переписать хендлеры для заданий 6-19 (`task_6_handler.py` и т.д.), чтобы они "ловили" `TaskCallback.filter(F.action == "select_task")` вместо старых `F.data == "gpt:task:X"`.

- [ ] **Модернизировать `theory_handler.py`:**
  - **Проблема:** Хендлер `theory_handler.py` (теперь в `group_1_5`) работает по старой `callback_data`.
  - **Что нужно сделать:** Переписать его, чтобы он реагировал на `TaskCallback` с `action="get_theory"`. Адаптировать логику для работы с `task_package`.

  - **Ключевой файл для рефакторинга:** `handlers/_legacy/navigation_handlers.py`. Этот файл является "сердцем" всей старой логики. При переписывании каждого нового задания (например, Задания 6), нужно будет:
    1. Создать для него новый `..._router.py` по образцу `task_1_5_router.py`.
    2. "Вырезать" из `navigation_handlers.py` старый обработчик (например, `on_task_6`).
    3. Постепенно этот файл должен "худеть", пока не исчезнет совсем.

## 💡 Идеи и Улучшения на Будущее

- [ ] **Улучшить скрипт `scripts/create_new_subtype.py`:**
  - **Идея:** Сделать его еще умнее, чтобы он не просто создавал пустые файлы, а наполнял их минимально необходимым кодом-шаблоном. (Мы это уже сделали, можно будет потом отметить как выполненное!)

- [ ] **Документация:**
  - [x] Создать структуру папок для `docs`.
  - [x] Заполнить `docs/tasks/task_1_5_group/README.md`.
  - [x] Заполнить `docs/tasks/task_1_5_group/tires/data_structure.md`.
  - [x] Заполнить `docs/01_architecture.md`.
  - [ ] Заполнить `docs/02_help_system.md`.
  - [ ] Заполнить `docs/tasks/task_1_5_group/tires/solver_logic.md`.

- [ ] **Реализовать "Решатели" для Помощи:**
  - [ ] Написать функции `solve_q2_tires`, `solve_q3_tires` и т.д. в `help_core/solvers/`.

  - [ ] **Улучшить парсинг `display_scenario`:**
  - **Проблема:** В `task_1_5_router.py` (в `dispatch_overview_screen`) тексты `intro` и `condition` "склеиваются" в одну переменную `task_text`.
  - **Что нужно сделать:** В будущем, когда понадобится более детальная работа с контекстом (например, в Помощи), стоит переписать этот код, чтобы `intro_text` и `condition_text` сохранялись в `state` как **отдельные** переменные.

  - [ ] **Провести рефакторинг "старых" заданий (6, 7, 8 и т.д.):**
  - **Проблема:** Логика для этих заданий сейчас находится в `handlers/_legacy/task_utils.py` и использует устаревшую архитектуру (смешение генерации, отправки и FSM в одном файле). Также проверить handlers\_legacy\task_loader.py, subtype_templates.py и bot_messages.py — это устаревшая система "копирайтинга", которую нужно будет заменить на новую, основанную на gpt/phrases/
  - **Что нужно сделать:** Переписать генерацию этих заданий по принципам "Идеального Завода", как мы сделали для Заданий 1-5. Создать для них свои `TaskGenerator`'ы, `Calculator`'ы и `..._router.py`. После этого `task_utils.py` можно будет удалить.
  
  !!!Постепенно перевести остальные задания, которые всё ещё используют after_task_keyboard, на вызовы get_after_task_keyboard, чтобы можно было удалить legacy-фоллбек.
  После миграции удалить _build_legacy_after_task_keyboard и перепроверить связанные колбэки.!!!!




  - [ ] **Реализовать "умный" `start_handler` и сохранение прогресса:**
  - **Проблема:** Сейчас команда `/start` всегда запускает процесс знакомства заново, что неудобно для вернувшихся пользователей.
  - **Задача (Уровень 1 - Обязательно):** Модифицировать `handlers/start.py`. При команде `/start` хендлер должен проверять, есть ли пользователь в БД `SQLite` и заполнено ли у него имя.
    - Если да: Отправить короткое приветствие ("С возвращением, {имя}!") и сразу показать главное меню.
    - Если нет: Запустить полный процесс онбординга (запрос имени и пола).
  - **Задача (Уровень 2 - На вырост):** В `state` пользователя сохранять последнее активное задание (например, `last_task = '1-5_tires'`). При повторном заходе предлагать ему кнопку "Продолжить с того же места".

---
*Этот журнал должен обновляться после принятия важных технических решений.*



Чек-лист: Результаты "Генеральной Уборки" Проекта "Матюня"
Статус: Завершена.
Цель: Устранить "мусор", дублирование кода, исправить "призрачные" модули и привести структуру проекта к единой, масштабируемой архитектуре.
1. Уборка в Корневой Папке
Проведена полная ревизия файлов в корне проекта.
УДАЛЕНЫ (как "мусор" и "призраки"):
<font color="red">run_accumulation.py</font> (старый "охотник" за GPT-данными)
<font color="red">load_skills.py</font> (старый "мигрант" для skill_types)
<font color="red">python</font> (случайный артефакт)
<font color="red">generate_plots.py</font> (старый "генератор-выдумщик")
<font color="red">compare_templates.py</font> (одноразовый "сравнитель")
<font color="red">db.json</font> (старая база данных TinyDB)
ПЕРЕМЕЩЕНЫ (в scripts/ и tests/):
<font color="green">set_bot_commands.py</font> → scripts/
<font color="green">get_bot_commands.py</font> → scripts/
<font color="green">test_calculator.py</font> → tests/task_generators/tasks_1_5/tires/
<font color="green">verify_answers.py</font> → scripts/audit_tasks_7.py
<font color="green">create_structure.py</font> → scripts/create_task_1_5_structure_example.py (и был улучшен)
ОСТАВЛЕНЫ (как критически важные): main_v2.py, matunya.db, README.md, .gitignore, .env, config.py.
Результат: Корневая папка теперь содержит только самые важные файлы и четко структурированные директории.
2. Реструктуризация Модулей и Хендлеров
Проведена глубокая реорганизация папок utils, handlers, gpt, tests и validators.
Созданы новые, логичные "дома":
_archive/: Для "музейных экспонатов", которые жалко удалять (пока пуста, но идея осталась).
handlers/_legacy/: "Капсула времени" для всей старой, но еще работающей архитектуры.
handlers/callbacks/navigation/: Новый дом для всех клавиатур и хендлеров, связанных с навигацией.
handlers/dialogs/: Новый, полноценный модуль для "Диалога с GPT".
keyboards/onboarding/: Отдельная папка для клавиатур процесса "знакомства".
core/callbacks/: "Ядро" проекта, где теперь живут определения CallbackData.
Проведены "слияния" и устранены дубликаты:
Два файла проверки ответов (answer_check.py и answer_utils.py) были объединены в один, супер-умный utils/answer_utils.py. <font color="red">answer_check.py</font> удален.
Два файла состояний (task_states.py и user_states.py) были объединены в один states/states.py. <font color="red">task_states.py</font> удален.
"Призраки" изолированы или удалены:
<font color="red">handlers/help_legacy.py</font> и <font color="red">handlers/help_flow.py</font> (две старые системы Помощи) удалены.
<font color="green">handlers/messages/</font> (и все ее содержимое) удалена, так как наша новая архитектура работает через FSM, а не через общие текстовые хендлеры.
<font color="green">task_utils.py</font>, <font color="green">task_loader.py</font>, <font color="green">task_repo.py</font>, <font color="green">bot_messages.py</font> и <font color="green">py_generators/</font> (вся старая архитектура) были перемещены в handlers/_legacy/.
"Специалисты" отправлены на "место работы":
<font color="green">image_generator.py</font> (Художник) → task_generators/task_7/.
<font color="green">svg_convert/</font> (Конвертер) → task_generators/tasks_1_5/apartment/renderers/.
<font color="green">validators/</font> (Контролер Качества) → task_generators/task_12/validators/.
3. Создание "Технического Журнала"
Создан и Наполнен: Файл docs/03_decisions_log.md стал нашим главным "TODO-листом". В него занесены все важные задачи на будущее:
Написать тесты для нового answer_utils.py.
Провести ревизию text_formatters.py.
Реализовать "умный" start_handler.
Провести полный рефакторинг "старых" заданий, избавившись от всего, что лежит в _legacy/.



📘 Бортовой журнал — “Операция Идеальная Чистота”

Фаза: 2
Цель: устранить остаточные дубли и внедрить единую систему управления сообщениями по всему боту.

🧭 1. Ручные delete() (точки внимания)
Файл	Место	Комментарий
handlers/parts_handlers.py:78	_send_formatted_message	Используется для удаления старого сообщения — заменить на cleanup_messages_by_category с тегом menus.
handlers/dialogs/handler.py:113	handle_dialog_message	Перевести на cleanup_messages_by_category(bot, state, chat_id, "dialog_messages").
handlers/_legacy/navigation_handlers.py	on_task_10	Проверить — если используется в актуальной логике, заменить на tracked-сообщения.
handlers/callbacks/navigation/main_menu_callbacks.py	back_to_main_menu_universal_handler	Заменить ручное удаление на cleanup_all_messages.
handlers/callbacks/task_handlers/task_10_handler.py	run_task_10_generation	Перевести на send_tracked_message.
💬 2. Прямые bot.send_message(...)
Файл	Место	Что сделать
help_core/dispatchers/task_1_5/help_handler_1_5.py	все блоки handle_help_dialog	Заменить на send_tracked_message, категория "help_panels".
navigation/main_menu_callbacks.py	back_to_main_menu_universal_handler	Перевести на send_tracked_message.
answer_handler.py	_handle_all_tasks_solved	Аналогично — заменить на tracked-отправку с категорией "notifications".
task_10_handler.py	генерация	Привести к архитектуре message_manager.
🧩 3. Повторные cleanup_messages_by_category(...)
Файл	Место	Комментарий
help_handler_1_5.py	hide_help_handler	Централизовать вызовы для help_panels и dialog_messages. Создать утилиту cleanup_help_panels().
dialogs/handler.py	on_end_dialog	Перевести на cleanup_dialog_messages().
answer_handler.py	_handle_correct_answer, _handle_all_tasks_solved	Проверить порядок уборки категорий (menus, notifications, focused_task_panel) — возможна оптимизация.
🧽 4. Повторные cleanup_all_messages(...)
Файл	Место	Что сделать
main_menu_callbacks.py	back_to_main_menu_universal_handler и back_to_parts_handler	Создать утилиту reset_to_main_menu(bot, state, chat_id), вызывающую cleanup_all_messages() + send_main_menu().
🧠 5. Централизация хелперов
Цель	Описание
send_tracked_message + send_tracked_photo	объединить в модуль message_senders.py с короткими алиасами.
cleanup_messages_by_category + cleanup_all_messages	вынести в cleanup_utils.py с понятными функциями: clear_menus, clear_help, clear_dialogs.
_remove_loading_message	сделать универсальным (remove_by_category("notifications")).
🪄 6. Дальнейшие шаги

Создать единый интерфейс “Clean&Send” — обёртку:

await clean_and_send(bot, state, chat_id, category="menus", text=..., keyboard=...)


чтобы объединить две операции в одну.

Применить по всем заданиям (6, 7, 11, 12) при показе каруселей.

Создать мини-тесты для message_manager (pytest).


🚀 ЧЕКЛИСТ ГОТОВНОСТИ К ЭТАПУ 2 “Централизация Message Manager”
№	Проверка	Статус
✅ 1	Все карусели заданий (1–5, 11) теперь очищают меню перед показом (cleanup_messages_by_category("menus"))	Готово
✅ 2	Все новые сообщения в этих сценариях отправляются только через send_tracked_message	Готово
⚙️ 3	parts_handlers.py всё ещё использует message.delete() — допустимо, но отметить для рефакторинга	Позже
⚙️ 4	В модулях help_handler, answer_handler, dialogs/handler используются прямые bot.send_message и ручные очистки	Отложено до Этапа 2
✅ 5	task_1_5_router.py и task_11_handler.py приведены к стандарту «чистоты» и не содержат дубликатов	Готово
⚙️ 6	В навигации (main_menu_callbacks) используется cleanup_all_messages() без централизатора reset_to_main_menu()	Сделать в Этапе 2
🧩 7	Не создана ещё универсальная обёртка clean_and_send() для двух операций — планируется	Сделать в Этапе 2
🧪 8	Нет unit-тестов для message_manager (отслеживание и очистка категорий)	Сделать в Этапе 2
✅ 9	Логика меню 1–5 / 11 синхронизирована: «любая кнопка задания → очистка menus → показ карусели»	Готово
⚙️ 10	Провести ревизию старых модулей task_10_handler, _legacy/navigation_handlers.py	Сделать после стабилизации


📘 Бортовой журнал — исправление #2025-10-05-A

Тема: Централизация кнопок навигации в финальном сообщении (1–5)

Файл:
matunya_bot_final/handlers/callbacks/task_handlers/group_1_5/answer_handler.py

Функция:
_handle_all_tasks_solved()

Суть правки:
Заменить ручные кнопки:

text="🔙 К выбору задания"
callback_data="back_to_task_type"
text="🔝 В главное меню"
callback_data="back_to_main"


на вызов централизованного блока:

from matunya_bot_final.keyboards.navigation.navigation import back_and_main_kb
for row in back_and_main_kb().inline_keyboard:
    final_keyboard.row(*row)


Статус: 💤 Отложено до проверки стабильности запуска бота.
Следующий шаг: Вернуться к этой правке после успешного запуска и теста навигации.



handlers/

callbacks/
dialogs/
gpt_dialog_control_handler.py — общий центр управления гибридным диалогом: следит за состоянием GPState.in_dialog, принимает вопросы ученика, формирует запросы к GPT с накопленной историей, отправляет ответы и утилизирует переписку по кнопкам ❓ Ещё вопрос / ✅ Всё понятно.
task_handlers/
group_1_5/
help_handler.py — фронтовой хендлер помощи для задач 1‑5: подготавливает и отправляет структурированную подсказку, очищает лишние клавиатуры, запускает диалог (запоминает solution_core и промпт, переводит FSM в GPState.in_dialog и отправляет первое приглашение).
task_11/
help_handler.py — аналогичный хендлер для задания 11: получает HTML-подсказку от диспетчера, обнуляет клавиатуры, записывает промпт/контекст и передаёт управление общему диалоговому контроллеру; отличается тем, что работает с solution_core нового формата и персонализированным промптом.
message_handlers/
group_1_5_answer_handler.py — обрабатывает текстовые ответы учеников для задач 1‑5: проверяет корректность, подсвечивает ошибки, обновляет UI после каждой попытки.
task_11_answer_handler.py — отвечает за проверку многошаговых ответов по заданию 11: сверяет по токенам, обновляет строку «Ответ: …» в основном сообщении, переключает клавиатуры по статусу (успех/повтор).
help_core

dispatchers/
task_1_5/help_handler_1_5.py — «специалист»‑диспетчер, который в конвейере помощи «подбирает решателя», запускает humanizer и готовит данные для фронтового хендлера (HTML‑текст + solution_core).
task_11/help_handler_11.py — похожий диспетчер для задания 11, но в «гибридном» стандарте: динамически импортирует нужный solver, нормализует solution_core, передаёт его humanizer’у и возвращает готовый HTML; ключевое отличие — строгий формат solution_core и связка с task_11_dialog_prompts.
prompts/
dialog_prompts.py — универсальный генератор системных промптов для задач 1‑5: учитывает имя/пол, историю и solution_core, выдаёт «дружелюбного Матюню» с HTML‑инструкциями.
task_11_dialog_prompts.py — специализированный промпт для задания 11: коротко напоминает структуру решения, учитывает имя/пол, запрещает повтор полного разбора.
solvers/ — решатели приведены к единому solution_core‑формату (question_id, explanation_idea, calculation_steps, final_answer, hints); каждая задача/подтип имеет модуль с функцией solve(task_package) и возвращает нормализованный словарь, который далее используют диспетчеры и humanizer.
keyboards

navigation/
help_dialog_navigation.py — центральное место для клавиатур помощи: содержит get_help_panel_keyboard (кнопки «Скрыть» / «Задать вопрос») и get_gpt_dialog_keyboard (кнопки завершения/продолжения диалога); решает проблему дублирования UI‑логики и гарантирует синхронное изменение клавиатур во всех сценариях помощи.
Этот обзор отражает текущую архитектуру: диспетчеры готовят контент, task‑хендлеры управляют UI и состояниями, gpt_dialog_control_handler ведёт диалог, а общие утилиты (prompts, solvers, keyboards) обеспечивают единый стиль и поведение.
