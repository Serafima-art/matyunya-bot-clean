# Документация: Модуль Заданий 1-5

## 1. Общее описание

Этот документ описывает внутреннюю архитектуру и логику работы **универсального конвейера** для генерации практико-ориентированных задач (Задания 1-5 ОГЭ).

Главная цель этой системы — обеспечить стабильность, масштабируемость и простоту добавления новых подтипов (Шины, Печи, Квартиры и т.д.), избегая ошибок, с которыми мы столкнулись на ранних этапах разработки.

---

## 2. Архитектура ("Идеальный Завод 2.0")

Система построена на принципе **полного разделения Данных и Кода**.

### 2.1. Папка с Данными (`data/tasks_1_5/<subtype>/`)

Содержит все "сырые" материалы, необходимые для сборки задачи.

-   **/plots/**: Папка с `.json`-файлами ("Коржи"). Каждый файл — это уникальный набор числовых данных для одного полного варианта (5 заданий). Содержит `base_tire_marking`, `allowed_tire_sizes`, `task_specific_data` и т.д.
-   **/texts/**: Папка с `.json`-файлами ("Ингредиенты"). Содержат шаблоны текстов:
    -   `intros.json`: Варианты вступлений.
    -   `conditions.json`: Варианты общих условий.
    -   `questions.json`: Варианты формулировок для всех типов вопросов (Q1-Q6).
    -   `lexemes.json`: Словарь для склонений "персонажей" (автомобиль, минивэн...).
-   **/assets/**: Папка со статичными изображениями (`.png`), общими для всех вариантов.

### 2.2. Папка с Кодом (`task_generators/tasks_1_5/<subtype>/`)

Содержит "инструменты", которые обрабатывают данные.

-   **config.py**: "Паспорт Цеха". Содержит пути ко всем данным и "специалистам", а также правила сборки для конкретного подтипа.
-   **generator.py**: "Главный Инженер". Универсальный `TaskGenerator`, который работает только по инструкциям из `config.py`.
-   **calculator.py**: "Бухгалтер". Чистый, тестируемый модуль, содержащий только математическую логику для расчетов.
-   **render_table.py**: "Художник". Чистый модуль, содержащий только логику генерации HTML-таблиц.

---

## 3. Процесс Генерации (Работа "Конвейера")

Процесс запускается, когда пользователь выбирает подтип задания.

1.  **Инициализация:** `TaskGenerator` "просыпается", читает `config.py` и загружает в память все пути, правила и "специалистов".

2.  **Случайный Выбор ("Кладовщик"):**
    -   Выбирается один случайный `plot.json` из папки `/plots/`.
    -   На основе правил из `plot.json` и `config.py` выбираются подходящие текстовые шаблоны (intro, condition, по одному для каждого из 5 вопросов).

3.  **Расчеты ("Бухгалтер"):**
    -   Выбранный `plot.json` (числа) передается в `calculator.py`.
    -   "Бухгалтер" производит все необходимые вычисления и возвращает словарь со всеми правильными ответами для 5 заданий.

4.  **Сборка `task_package` ("Упаковщик"):**
    -   **Форматирование:** Для каждого из 5 вопросов собирается полный "контекст" (общие данные + `task_specific_data` из `plot.json`), и плейсхолдеры в текстовом шаблоне заменяются на числа.
    -   **Сборка "Сценария":** `display_scenario` собирается в строгом порядке: 2 картинки, intro, condition.
    -   **Сборка "Задач":** Список `tasks` (из 5 объектов) собирается отдельно. В каждый объект задачи кладется ее текст и правильный ответ, полученный от "Бухгалтера".
    -   **Прикрепление Таблиц:** "Художник" (`render_table.py`) генерирует HTML-таблицы, которые прикрепляются к нужным задачам (таблица размеров к Q1, таблица сервисов — к Q5/Q6).

5.  **Регистрация в БД:**
    -   Готовый `task_package` "пробегается" по 5 задачам, регистрирует каждую в базе данных и сохраняет полученные `db_task_ids`.

Этот "чертеж" является основой для всех практико-ориентированных задач.


Привет!

Ты участвуешь в консилиуме ИИ по созданию "Золотых Наборов" — лучших объяснений и метафор для Telegram-бота "Матюня". "Матюня" — дружелюбный репетитор, который помогает школьникам готовиться к ОГЭ по математике.

Сейчас ты — опытный педагог-методист и креативный автор. Ты умеешь объяснять сложные вещи просто, ярко и с юмором, чтобы подростки 15-17 лет их запоминали.

🎯 Твоя задача: Для подтипа Шины задания 1-5 придумать 5-7 уникальных, ярких и полезных фраз-подсказок. Это не должны быть полноценные карточки, а именно "россыпь жемчуга" — короткие, запоминающиеся идеи, метафоры и алгоритмы.

📦 Требования к каждой фразе:

Стиль: В духе "Матюни" — дружелюбный, понятный, поддерживающий. Используй метафоры, сравнения, юмор, жизненные аналогии.

Цель: Фраза должна помогать ученику понять ключевую идею для решения подтипа.

⚠️ Важные правила:

Не повторяйся. Каждая фраза должна нести новую мысль или метафору.

Пиши просто, без заумных терминов.

Запрещено использовать LaTeX и другие технические обозначения. Используй обычные символы, которые используются в файле PDF. Знаки +, -, ·, ÷

🎯 Цель консилиума: Собрать богатую "палитру стиля" для каждого подтипа. Позже эти фразы будут загружены в Google-таблицу и станут основой для ответов GPT в режиме Помощи.

Задание 1-5:

Подтип: Шины.

Сгенерируй 5-7 фраз с объяснением для этого подтипа для шести вопросов Q1-Q6.

‼️ ВАЖНО: Формат Вывода
Выводи результат в следующем формате, группируя фразы по подтипам:

=== tires_q1 ===
1. Фраза-подсказка номер один для Q1.
2. Фраза-подсказка номер два для Q1.
3. Фраза-подсказка номер три для Q1.

=== tires_q2 ===
1. Фраза-подсказка номер один для Q2.
2. Фраза-подсказка номер два для Q2.
3. Фраза-подсказка номер три для Q2.

(и так далее)
