# 🛠 TECH DEBT / TODO

## 🔁 Унификация ключа текста задания (Block 1–5)

### 📌 Текущее состояние (временное решение)

В проекте используются два ключа для текста задания:

- `question_text` — для подтипа **paper**
- `text` — для подтипа **tires**

В `task_1_5_router.py` временно используется совместимый вариант:

```python
task.get("question_text") or task.get("text", "")

Это сделано сознательно, чтобы:

✅ Paper работал по новой архитектуре

✅ Шины не сломались

❗ Не блокировать текущую разработку

🎯 План миграции (после реализации всех non_generators 1–5)

Когда все подтипы блока 1–5 будут переведены на схему:

validator → JSON → runtime → state → handler

Необходимо:

Перевести tires на question_text

Обновить валидатор шин

Обновить runtime шин

Обновить JSON шин

Удалить поддержку text из router

Зафиксировать единый контракт:

question_text = единственный допустимый ключ
⚠ Важно

После миграции удалить костыль:

task.get("question_text") or task.get("text", "")

и оставить только:

task.get("question_text", "")

📅 Добавлено: 20.02.2026
👩‍💻 Ответственный: Оленька + Архитектор
