📘 Правило хранения переменных и метаданных в задачах задания 8

(Единый стандарт для integer_expressions и powers_and_roots)

🎯 1. Принцип единого источника истины

В заданиях с выражениями со степенями и корнями единственным источником структуры выражения является expression_tree.

Решатель никогда не должен использовать какие-либо другие данные о степенях, множителях или структуре выражения.

📦 2. Какие данные сохраняются в финальный JSON задачи

В поле "variables" разрешены только реальные переменные задачи, то есть те, которые присутствуют в выражении и используются при вычислении ответа:

"variables": {
    "a": 10,
    "b": 14,
    "solution_pattern": "power_fraction_basic"
}

Разрешено:

a, b, c — числовые значения переменных

solution_pattern — если подтип содержит несколько методик решения

🚫 3. Какие данные запрещено включать в финальный JSON

Запрещено хранить любые параметры, использованные на этапе генерации:

❌ степени: m, n, r, s, p, q
❌ коэффициенты паттерна
❌ параметры случайной генерации
❌ промежуточные вычисленные степени
❌ дублирующие поля, которые повторяют содержимое дерева

Почему запрещено?

Потому что такие поля дублируют структуру, создают риск рассинхронизации и могут привести к неверной работе решателя.

📘 4. Почему именно так — архитектурное обоснование
✔ Решатель должен опираться только на:

expression_tree — структура выражения

variables — значения переменных

solution_pattern — маршрут для выбора метода

✔ Все степени и операции решатель получает только из дерева

Это исключает ситуацию, когда:

в дереве exp: 7,

а в variables забыли исправить r: 6.

Такой рассинхрон — причина большинства ошибок в ранних версиях Matyunya (особенно в задании 6).

🏭 5. Где хранятся m, n, r, s и прочие метаданные

Эти данные остаются только:

в definitions/*.txt (сырьё),

внутри валидатора, когда он строит expression_tree,

внутри генератора, который создаёт строки-сценарии.

Но они не должны попадать в финальную БД с задачами.

🧩 6. Финальный JSON-задачи (эталон)
{
    "id": "8_alg_power_fraction_x2j9k1",
    "task_type": "8",
    "subtype": "alg_power_fraction",
    "solution_pattern": "power_fraction_basic",
    "expression_tree": {
        "type": "fraction",
        "numerator": {
            "type": "product",
            "items": [
                { "type": "pow", "base": "a", "exp": 26 },
                { "type": "pow", "base": "b", "exp": 24 }
            ]
        },
        "denominator": {
            "type": "pow",
            "base": { "type": "product", "items": [ "a", "b" ] },
            "exp": 24
        }
    },
    "variables": {
        "a": 11,
        "b": 16
    },
    "answer": 11
}

❤️ Финальная фиксация стандарта

В финальном JSON задачи задания 8 хранятся только реальные переменные и expression_tree.
Параметры генерации (m, n, r, s, k_i) полностью исключаются.
Единственным источником структуры выражения является дерево.
Решатель не опирается на никаких дополнительных полей.
