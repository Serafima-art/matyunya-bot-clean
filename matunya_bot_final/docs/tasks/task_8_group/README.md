📘 Expression Tree — Финальный стандарт (Задание 8, Тема 1: Integer Expressions)

Этот стандарт описывает единую структуру expression_tree,
которая используется всеми тремя паттернами темы 1:

alg_power_fraction

alg_radical_power

alg_radical_fraction

Один решатель и один humanizer работают со всеми формами выражений,
потому что дерево строго унифицировано.

🔧 1. Базовые принципы

Структура вложенная (nested) — каждая операция является отдельным узлом-объектом.

Все вычисления производятся только по дереву, никаких параллельных данных.

В дереве могут встречаться только те типы узлов, которые перечислены ниже.

Поле variables содержит только значения переменных.

Поле solution_pattern определяет, какое внутреннее решение выполняет решатель.

🌿 2. Типы узлов дерева

Каждый узел имеет поле type.
Допустимые значения:

✔ integer

Целое число (внутри выражения).

{ "type": "integer", "value": 25 }

✔ variable

Переменная.

{ "type": "variable", "name": "a" }

✔ power

Степень: baseᵉˣᵖ

{
  "type": "power",
  "base": { ... },
  "exp": 7
}


Допускается вложенность (power внутри power) — «башни».

✔ product

Произведение множителей.

{
  "type": "product",
  "factors": [ ... ]
}


Порядок множителей сохраняется.

✔ fraction

Дробь.

{
  "type": "fraction",
  "numerator": { ... },
  "denominator": { ... }
}

✔ sqrt

Корень над выражением.

Используется строгое имя поля: radicand — подкоренное выражение.

{
  "type": "sqrt",
  "radicand": { ... }
}

📑 3. Структура полного JSON объекта задачи

Каждый объект в итоговой БД data/tasks_8.json имеет вид:

{
  "id": "8_integer_expressions_001",
  "task_type": "8",
  "subtype": "integer_expressions",

  "solution_pattern": "alg_power_fraction",

  "expression_tree": { ...см. эталон... },

  "variables": {
    "a": 10,
    "b": 15
  },

  "answer": "0.004"
}

🔍 4. Требования к структуре
✔ Единый формат дерева

Одинаковый для всех паттернов — дробей, степеней, корней, комбинаций.

✔ Никаких дополнительных полей

В финальном JSON нет m, n, r, s, K1, K2 и других метапараметров —
все степени и коэффициенты берутся только из expression_tree.

✔ Поле solution_pattern обязательно

Позволяет одному решателю обслуживать все подтипы integer_expressions.

🧩 5. Примеры эталонов

Три эталонных выражения для трёх паттернов (коротко):

⭐ alg_power_fraction

(сложные дроби со степенями)

{
  "type": "fraction",
  "numerator": { "type": "product", "factors": [ ... ] },
  "denominator": { "type": "power", "base": { ... }, "exp": 7 }
}

⭐ alg_radical_power

(один корень, накрывающий дробь/произведение)

{
  "type": "sqrt",
  "radicand": {
    "type": "fraction",
    "numerator": { ... },
    "denominator": { ... }
  }
}

⭐ alg_radical_fraction

(несколько корней сверху и снизу)

{
  "type": "fraction",
  "numerator": { "type": "product", "factors": [ ... ] },
  "denominator": {
    "type": "sqrt",
    "radicand": { "type": "product", "factors": [ ... ] }
  }
}

🎯 6. Основной вывод

Мы используем единый expression_tree для всех трёх паттернов,
что даёт:

один решатель

один humanizer

нулевую вероятность конфликтов

удобство генерации

надёжность для любого будущего расширения
