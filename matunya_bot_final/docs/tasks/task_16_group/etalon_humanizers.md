✅ Что правильнее для cyclic_quad_angles
Правильный формат: ключевой, как в Task 8

но более строгий:

1) Один верхний роутер по narrative_type

opposite_sum

part_sum

part_diff

2) У каждого narrative_type — фиксированный набор шаблонов

idea_template

steps_templates (2–4 коротких шага под мобилку)

answer_template

tips_template (общие “бантик/ОГЭ”)

3) Никаких if/endif и regex-обработки

Если нужно “иногда показывать дугу/части” — решается на уровне валидатора, который гарантирует поля.
Хьюмонайзер не должен “угадывать”.

🧱 Рекомендованная структура (без кода, но канон)

Словарь “профилей” по narrative_type:

NARRATIVE_PROFILES[narrative_type] = {

idea_key

steps_keys: [ ... ]

tips_keys: [ ... ]

required_fields: [...] ← важно для тестов/валидации
}

Словари шаблонов:

IDEA_TEMPLATES

STEP_TEMPLATES

TIPS_TEMPLATES

ANSWER_TEMPLATE (единый)

Какой стиль шагов (под мобилку)

Для твоих углов идеально:

Шаг 1: Дано / Найти

Шаг 2: Равные углы на дуге (бантик)

Шаг 3: Сложить или вычесть

Ответ

Без “самопроверок”, без длинных абзацев.

Итоговый ответ на твой вопрос

✅ Правильный — ключевой подход (как Task 8), но жёстче и чище.
❌ Сценарный (как Task 15) — не надо. Он и создаёт legacy-контракт.


📌 Резюме решения (# 🟨 ТЕМА 2. `Касательная, хорда, секущая, радиус (circle_elements_relations)`, ## Паттерн 2.2 tangent_trapezoid_properties
)

Мы разделяем нарратив tangent_trapezoid_find_midline на два самостоятельных narrative:

tangent_trapezoid_find_midline_via_sides
Задачи, где известны боковые стороны описанной трапеции.
Средняя линия находится через свойство:

сумма оснований = сумма боковых сторон.

tangent_trapezoid_find_midline_via_bases
Задачи, где основания даны напрямую.
Средняя линия находится по формуле, без дополнительных рассуждений.

Почему так

Один narrative = один когнитивный сценарий.

Убираем subtype и любую условную логику в humanizer.

Каждый нарратив имеет фиксированный набор шагов и полей.

Humanizer ничего не угадывает, не ветвится, не падает.
