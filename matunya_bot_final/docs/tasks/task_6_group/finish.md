📘 ФИНАЛЬНЫЙ ДОКУМЕНТ ПО ЗАДАНИЮ №6 (ОГЭ-МАТЕМАТИКА)
Полное закрытие модуля «Помощь» + описание всех болезненных мест, ошибок, побед и правил расширения
✅ 1. Что мы сделали и довели до идеала

Модуль «Помощь» для задания 6 теперь:

✔️ полностью рабочий

– 157/157 тестов проходят зелёным, без единой ошибки.
– Решатели дают корректные вычисления и последовательные шаги.
– Humanizer формирует чистый, устойчивый HTML, готовый к Telegram.

✔️ полностью универсальный

Поддержано 4 паттерна:

cf_addition_subtraction

multiplication_division

parentheses_operations

complex_fraction

Каждый работает независимо, без взаимного влияния.

✔️ Устранены все архитектурные дефекты

– Мы заменили старые закостыленные схемы.
– Вычистили рекурсию, вложенные костыли, опасные преобразования.
– Убрали повторяющийся код, ошибки типов, ошибки сокращения дробей самими Fraction.

🔥 2. Все боли, с которыми мы столкнулись (и больше никогда не должны сталкиваться)
🔥 Боль №1. Дерево expression_tree работало непредсказуемо

– Иногда узел содержал "operation", иногда нет.
– Иногда узел был числом, иногда словарём, иногда вообще странной структурой.
– Из-за этого возникали все KeyError: 'operation', 'operands', 'type'.

Как решили:
→ создали гарантированную схему выбора inner_node с фильтрацией по "operation".

🔥 Боль №2. Перекос смешанных чисел

– Узлы типа "text": '3 1/8' не совпадали с "value"
– Fraction сам сокращал дроби → шаг «сокращение» ломался.

Как решили:
→ принудительно фиксируем value = [num, den] после преобразования, отключив автоматизм Fraction.
→ восстановили честный шаг сокращения.

🔥 Боль №3. Паттерн parentheses_operations был смертельно хрупким

– Разные виды скобок:
(A ± B) ⋅ C
(A) ⋅ C
Смешанные числа внутри
Смешанные числа снаружи
– Всё это рушило внутреннюю логику.

Как решили:
→ написали универсальный предикат valid_inner(…)
→ добавили fallback: если inner_node не содержит операцию — поднимаемся на уровень выше.
→ стабилизировали выбор inner_node окончательно.

🔥 Боль №4. Generators не используются в этом задании → дерево приходило из валидатора

И валидатор не гарантировал корректную структуру.

Как решили:
→ создали правила проверки и исправления дерева на входе решателя.
→ написали стресс-тест, который гонит все 157 задач через solver → humanizer → HTML.

🔥 Боль №5. Fraction сокращал дроби раньше времени

Это ломало шаг CF_REDUCE_FINAL.

Как решили:
→ везде контролируем numerator/denominator и приводим к несокращённому виду вручную.

🔥 Боль №6. CONVERT_TO_DECIMAL отсутствовал в некоторых паттернах

В complex_fraction и parentheses_operations.

Как решили:
→ добавили единый шаг «Перевести результат в десятичную дробь» в конце каждого паттерна.

🔥 Боль №7. Внутренние типы "integer" ломали скобочные вычисления

(Например: 42 ⋅ (3/14 − 4/21)).
Целое число не имеет denominators → вычисления ломались.

Как решили:
→ теперь integer → Fraction(n, 1) всегда.

🔥 Боль №8. Микро-ошибки в логике LCM, знаков, формул

Теперь всё консистентно, проверки полностью покрывают.

🔮 3. Правила на будущее при расширении задания 6
✔️ Правило №1. ДЕРЕВО ДОЛЖНО БЫТЬ ЧЁТКИМ

Каждый узел ОБЯЗАН иметь структуру:

{
  "operation": "add" | "subtract" | "multiply" | "divide",
  "operands": [node1, node2]
}


Лист (простой узел):

{ "type": "common", "value": [num, den], "text": "…" }
или
{ "type": "integer", "value": n, "text": "n" }


Никаких других форматов.

✔️ Правило №2. Смешанные числа всегда преобразуются вручную

→ никакой автоматической магии Fraction.
→ node["value"] = [new_num, new_den].

✔️ Правило №3. Операции ADD, SUB, MULT, DIV — только через наш LCM+шаги

Никаких готовых Fraction(left)+Fraction(right).

✔️ Правило №4. ВСЕГДА делаем шаг «сокращение»

Даже если дробь уже сокращена — мы обязаны проверить.

✔️ Правило №5. ВСЕГДА делаем шаг «перевод в десятичную дробь»

Если denominator != 1.

✔️ Правило №6. Всегда писать тест, прогоняющий все задачи

Теперь у нас есть эталон — test_task6_help_system.py.

🌟 4. Что мы получили в итоге
🔥 Абсолютно устойчивый модуль
🔥 Который не падает ни на одной задаче
🔥 Который идеально оформляет решения
🔥 И который можно использовать как ЭТАЛОН для всех будущих модулей Помощи

Оленька, ты проделала гигантский путь.
Твой Матюня теперь стал намного сильнее.
И я так горжусь нашей с тобой командной работой ❤️
