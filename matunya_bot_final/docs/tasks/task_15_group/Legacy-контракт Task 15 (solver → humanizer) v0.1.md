✅ Legacy-контракт Task 15 (solver → humanizer) v0.1
0) Цель контракта

Обеспечить стабильную связку:

валидатор/сырьё (task JSON) → solver → humanizer → HTML-сообщение

без требования solution_core v2.2.

1) Вход solver’а

Solver получает задачу из БД в формате “task JSON” (как у вас сейчас):

id, pattern, narrative, text, answer, image_file (может быть null)

variables:

given (сырые данные)

to_find (что искать)

humanizer_data (всё, что нужно для текста/букв/символов)

📌 Правило: solver не обязан нормализовать задачу до ГОСТ-solution_core.
📌 Правило: всё “что показать” берём из given/humanizer_data (сырое), всё “что считать” — нормализуем в расчётной части solver’а.

2) Выход solver’а (главное правило)

Solver возвращает список действий, где каждое действие — dict:

[
  {
    "action": "<pattern>:<narrative>",
    "data": { ... контекст для humanizer ... }
  }
]

2.1 Поле action

Формат строго:

"{task['pattern']}:{narrative}"

Примеры:

"isosceles_triangle_angles:find_base_angle"

"isosceles_triangle_angles:find_vertex_angle"

"triangle_area_by_parallel_line:area_by_similarity"

📌 Humanizer маршрутизирует шаблон только по action.

3) Поле data (контекст) — обязательные минимумы

В каждом data должны быть:

3.1 Универсальные ключи

res — финальный ответ (обычно task["answer"], число или строка)

to_find_name или to_find_role (если используется шаблонами)

всё, что humanizer обязан вывести в “Дано/Найти”, должно лежать в data в явном виде, строками.

📌 Важно: Humanizer не “догадывается” и не “считает”. Он тупой официант.

3.2 Числа и формат

Все числовые значения, которые пойдут в HTML, должны быть:

уже преобразованы в строку через format_number(...)

без None в местах, где шаблон ждёт число

📌 Если величины может не быть — ключ всё равно можно передать, но шаблон должен уметь это пережить (или solver обязан выбрать другую ветку).

4) Внутреннее правило solver’а: Display vs Calc

Solver разделяет данные:

Display-данные: в data как “Дано” (сырые, без дедукции)

Calc-данные: во внутреннем словаре (s), где можно:

нормализовать имена (CN→NC, MA→AM)

вычислять недостающие куски

выводить итог

📌 Запрещено: подмешивать вычисленные значения в “Дано”.

5) Narrative = форма решения (обязательная классификация)

task["narrative"] (или вычисленный narrative) определяет форму и, значит, набор ключей, который solver обязан положить в data.

То есть для каждого pattern:narrative есть “мини-контракт”.

Пример: isosceles_triangle_angles:find_base_angle должно давать буквы вершин/основания и угол при вершине так, чтобы humanizer мог собрать:

свойство равнобедренного

сумма углов 180°

вычисление базового угла

6) Ответ

data["res"] — единственный источник ответа для humanizer.

📌 Ответ не вычисляет humanizer.
Он только выводит res в финальной строке.

7) HTML-оформление (напоминание)

Humanizer:

принимает action и data

генерирует HTML:

жирным выделяет числа/выражения

финал: <u><b>Ответ: ...</b></u>

“Частые ошибки” — <tg-spoiler> (если есть)

📌 Solver может передавать common_mistakes / errors_spoiler и т.п. — но это опционально.

8) Нулевая зависимость от solution_core

Для Task 15 не требуется:

explanation_idea

calculation_steps[]

final_answer{}

Это будет сделано позже отдельным рефактором.

9) Определение “Контракт соблюдён”

Считаем, что контракт соблюдён, если для каждого тестового задания:

solver вернул list с одним dict

action соответствует существующему шаблону humanizer’а

data содержит все ключи, которые шаблон читает

humanizer не падает и выдаёт HTML
