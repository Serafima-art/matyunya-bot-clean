# 🤖 Матюня - Telegram бот для подготовки к ОГЭ по математике

Архитектура **Гибридный Интеллект** с режимами накопления и боевым режимом для генерации геометрических задач ОГЭ.

## 📋 Описание проекта

**Матюня** - это интеллектуальный помощник для учеников 9 класса, готовящихся к ОГЭ по математике. Бот генерирует задачи по геометрии, создает чертежи и предоставляет пошаговые решения.

### 🎯 Основные возможности

- ✅ **Генерация задач** - 5 типов геометрических задач ОГЭ
- ✅ **Визуализация** - автоматическое создание чертежей с подписями
- ✅ **Пошаговые решения** - подробные объяснения с формулами
- ✅ **Соответствие ФИПИ** - все задачи соответствуют стандартам
- ✅ **Адаптивная сложность** - легкие, средние, сложные задачи
- ✅ **Система отчетов** - сохранение результатов в JSON

## 📁 Структура проекта

```
matunya_bot_final/
├── 🤖 __init__.py                    # Главный модуль
├── core/
│   ├── generators/
│   │   ├── __init__.py
│   │   └── task_generator.py         # Основной генератор задач
│   └── templates/
│       ├── __init__.py
│       └── geometry_texts.py         # Шаблоны и тексты
├── task_generators/
│   └── task_15/
│       ├── __init__.py
│       └── trig_generator.py         # Генератор тригонометрии
├── help_core/
│   └── solvers/
│       └── task_15/
│           ├── __init__.py
│           └── trig_calculator.py    # Тригонометрический калькулятор
├── visualization/
│   ├── __init__.py
│   └── geometry_visualizer.py        # Создание чертежей
├── data/
│   └── plots/
│       └── trigonometry_values.json  # База данных тригонометрии
├── telegram_bot_example.py           # Пример интеграции с ботом
├── demo_integration.py               # Демонстрация работы
└── README.md                         # Эта документация
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install matplotlib numpy telegram-bot python-telegram-bot
```

### 2. Тестирование системы

```bash
cd matunya_bot_final
python demo_integration.py
```

### 3. Интеграция с Telegram

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен
3. Замените `YOUR_BOT_TOKEN_HERE` в `telegram_bot_example.py`
4. Запустите бота:

```bash
python telegram_bot_example.py
```

## 🎯 Использование

### Базовое использование генератора

```python
from matunya_bot_final.core.generators.task_generator import MatyunyaGenerator

# Инициализация
generator = MatyunyaGenerator()

# Генерация задачи
task_data = generator.generate_task("trigonometry", "medium")
task = task_data['task']
print(f"Задача: {task['text']}")

# Решение задачи
solution = generator.solve_task(task)
print(f"Ответ: {solution['final_result']}")

# Полная генерация с визуализацией
result = generator.generate_with_visualization("trigonometry", "easy")
print(f"Изображение: {result['image_path']}")
```

### Создание собственного генератора

```python
from matunya_bot_final.task_generators.task_15.trig_generator import TrigonometryGenerator

generator = TrigonometryGenerator()

# Генерация различных типов задач
task1 = generator.generate_task("easy")
task2 = generator.generate_task("medium") 
task3 = generator.generate_task("hard")

print(task1['text'])  # Простая задача
print(task2['text'])  # Задача средней сложности
```

### Использование калькуляторов

```python
from matunya_bot_final.help_core.solvers.task_15.trig_calculator import TrigCalculator

calculator = TrigCalculator()

# Расчет по известному cos
result = calculator.calculate_from_cos("√3/2", 30, "find_sin")
for step in result['steps']:
    print(f"Шаг {step.step_number}: {step.formula}")
    print(f"Вычисление: {step.calculation}")

print(f"Ответ: {result['final_result']}")
```

### Создание визуализаций

```python
from matunya_bot_final.visualization.geometry_visualizer import GeometryVisualizer

visualizer = GeometryVisualizer()

# Создание треугольника
A = (1, 1)
B = (3, 1)  
C = (2, 2.732)

image_path = visualizer.create_triangle(
    A, B, C,
    title="Треугольник ABC\n∠A = 30°",
    show_lengths=True,
    show_angles=True,
    filename="my_triangle.png"
)

# Добавление биссектрисы
bisector_path = visualizer.add_bisector(A, (A, B, C), "my_bisector.png")
```

## 🎮 Команды бота

- `/start` - начать работу с ботом
- `/task` - сгенерировать задачу (средняя сложность)
- `/easy` - легкие задачи
- `/medium` - задачи средней сложности  
- `/hard` - сложные задачи
- `/help` - справка

Также можно писать текстовые команды:
- "задача" - сгенерировать задачу
- "легко" - простая задача
- "сложно" - сложная задача

## 📊 Типы задач

### 1. Тригонометрия
- Нахождение sin, cos, tg по углу
- Нахождение сторон через тригонометрию
- Работа со стандартными значениями

### 2. Углы
- Биссектрисы углов
- Внешние углы треугольника
- Сумма углов треугольника

### 3. Площади
- Площадь треугольника
- Отношение площадей
- Формула Герона

### 4. Подобие
- Коэффициент подобия
- Соответствующие стороны
- Площади подобных треугольников

### 5. Равносторонние треугольники
- Связь стороны и площади
- Высота и площадь
- Углы в равностороннем треугольнике

## 🔧 Конфигурация

### Настройка путей сохранения

```python
# Изменение директории для изображений
generator = MatyunyaGenerator(save_dir="/path/to/images")

# Изменение директории для визуализатора  
visualizer = GeometryVisualizer(save_dir="/custom/path")
```

### Добавление новых типов задач

1. Создайте генератор в `task_generators/task_15/`
2. Создайте калькулятор в `help_core/solvers/task_15/`
3. Добавьте шаблоны в `core/templates/geometry_texts.py`
4. Зарегистрируйте в `core/generators/task_generator.py`

### Настройка сложности

```python
# Легкие задачи - только базовые углы 30°, 45°, 60°
task_easy = generator.generate_task("easy")

# Средние задачи - больше вариаций
task_medium = generator.generate_task("medium")

# Сложные задачи - углы 15°, 75°, 105° и т.д.
task_hard = generator.generate_task("hard")
```

## 📈 Архитектура проекта

### Гибридный Интеллект

1. **Режим накопления** (офлайн):
   - Подготовка шаблонов задач
   - Создание базы знаний
   - Формирование складов креатива

2. **Боевой режим** (онлайн):
   - Генерация задач в реальном времени
   - Создание визуализаций
   - Пошаговые решения

### Соответствие ФИПИ

Все задачи соответствуют требованиям:
- ✅ Стандартные углы 30°, 45°, 60°
- ✅ Точные значения с корнями
- ✅ Формулировки по ФИПИ
- ✅ Соответствующая сложность

## 🛠️ Устранение неполадок

### Ошибки импорта

```python
# Проверьте структуру импортов
import sys
sys.path.append('/path/to/matunya_bot_final')

# Импорт модулей
from matunya_bot_final.core.generators.task_generator import MatyunyaGenerator
```

### Проблемы с визуализацией

```python
# Убедитесь, что matplotlib установлен
import matplotlib.pyplot as plt

# Проверьте права доступа к папке
import os
print(os.getcwd())
print(os.path.exists("visualization/examples"))
```

### Ошибки Telegram

```python
# Проверьте токен бота
import os
BOT_TOKEN = os.getenv('MATYUNYA_BOT_TOKEN')
if not BOT_TOKEN:
    print("Ошибка: Токен не найден")
```

## 📚 Дополнительные ресурсы

- [Документация python-telegram-bot](https://python-telegram-bot.org/)
- [Matplotlib документация](https://matplotlib.org/stable/contents.html)
- [Требования ФИПИ к ОГЭ](https://fipi.ru/)
- [Примеры задач ОГЭ по математике](https://math.oge.sdamgia.ru/)

## 📄 Лицензия

Этот проект создан для образовательных целей. Используйте свободно для обучения и подготовки к ОГЭ.

## 🤝 Вклад в проект

Приветствуются улучшения:
- Новые типы задач
- Улучшение алгоритмов
- Дополнительные визуализации
- Расширение базы знаний

---

**Удачной подготовки к ОГЭ! 🎓✨**