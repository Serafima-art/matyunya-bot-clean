СТАНДАРТ РЕАЛИЗАЦИИ ЗАДАНИЙ ЧАСТИ 2 (20-25) — Версия 2.0 (Финальная)
Статус: УТВЕРЖДЕНО
Дата: 13.10.2025
1. Философия: Унификация и Честный Выбор
Ключевой принцип реализации заданий Части 2 — полная унификация пользовательского опыта с заданиями Части 1 (6-19). Мы отказываемся от уникальных, сложных механик в пользу простоты, надежности и предсказуемости.
Основной тезис: Матюня — это не контролер, а помощник. Он предоставляет ученику инструменты и свободу выбора, возлагая на него ответственность за собственное обучение.
2. Отвергнутые Концепции
В ходе обсуждения были предложены и отвергнуты следующие подходы. Мы не возвращаемся к их рассмотрению в будущем, чтобы не тратить время на заведомо тупиковые или избыточно сложные пути.
2.1. Отказ от "Проверки по Фото"
Идея: Ученик решает задачу на бумаге, отправляет фото, и только после этого получает решение.
Причина отказа:
Техническая ненадежность: Сложность реализации ConversationHandler, потенциальные баги.
Проблема "обмана": Ученики могут отправлять любые изображения, что обесценивает педагогическую идею и подрывает репутацию бота.
Барьер для пользователя: Необходимость фотографировать и отправлять может быть неудобной.
2.2. Отказ от Рендеринга Решений в Изображение
Идея: Генерировать картинку с пошаговым решением, используя LaTeX.
Причина отказа:
Высокая хрупкость: Зависимость от внешних программ (LaTeX) и системных шрифтов.
Проблемы с качеством: Сложность в подборе универсальных шрифтов, размеров и DPI для всех устройств.
Низкая доступность: Текст на картинке нельзя скопировать.
2.3. Отказ от "Обязательного Ввода Ответа"
Идея: Скрыть кнопку "Помощь" и показывать решение только после того, как ученик введет свой вариант ответа.
Причина отказа:
Проблема ввода: Ответы на задания Части 2 часто содержат сложные символы (интервалы, корни, системы), которые невозможно ввести со стандартной клавиатуры.
Противоречие принципу унификации: Этот сценарий кардинально отличается от сценария для Части 1.
3. Финальный Утвержденный Сценарий
Для всех заданий Части 2 (20-25) реализуется стандартная механика "Помощь по кнопке", идентичная заданиям 1-19.
Выдача Задачи: Бот отправляет задачу со стандартной inline-клавиатурой, содержащей кнопки "🆘 Помощь", "📚 Теория", "🎯 Другое задание" и "💫 Назад".
Запрос Помощи: Ученик в любой момент может нажать кнопку "🆘 Помощь".
Вывод Решения: В ответ на нажатие бот присылает полное, текстовое, отформатированное HTML-сообщение с пошаговым решением. Рекомендуется использовать тег <tg-spoiler> для сокрытия всего блока решения.
Диалог с Экспертом: Под решением располагается клавиатура с кнопкой "❓ Задать вопрос", которая запускает диалог с GPT для разбора непонятных моментов.
4. Архитектурные Стандарты Реализации
Реализация "Помощи" для Части 2 строго следует уже существующим и апробированным стандартам, эталоном для которых является архитектура "Помощи" для Задания 11.
4.1. "ГОСТ-РЕШАТЕЛЬ-2025"
Расположение: matunya_bot_final/help_core/solvers/task_{N}/{subtype}_solver.py
Требования: Каждый "Решатель" должен быть асинхронной функцией solve(task_data: dict), которая принимает данные о задаче и возвращает один словарь solution_core.
4.2. "ГОСТ-2025: Стандарт solution_core"
Требования: Возвращаемый "Решателем" словарь solution_core должен строго соответствовать утвержденной структуре. Он содержит "сырые", неформатированные данные: explanation_idea, calculation_steps, final_answer, hints и другие обязательные поля.
4.3. "Диспетчер" и "Очеловечиватель"
Расположение: matunya_bot_final/help_core/dispatchers/task_{N}/help_handler_{N}.py
Требования: Для каждого задания создается свой "Диспетчер", который:
Динамически импортирует и вызывает нужный "Решатель" для конкретного subtype.
Содержит внутреннюю функцию _humanize_solution, которая преобразует "сырой" solution_core в готовый для отправки HTML-текст.
Этот документ фиксирует наш финальный курс. Все дальнейшие разработки по заданиям Части 2 должны вестись в строгом соответствии с этим стандартом.


Эталонный JSON-массив для tasks_20.json (подтип polynomial_factorization)
code
JSON
[
  {
    "id": "20_polynomial_factorization_213b78",
    "task_number": 20,
    "topic": "algebraic_expressions",
    "subtype": "polynomial_factorization",
    "question_text": "Решите уравнение:\n(x + 8)(x² + 3x + 2) = 3x² + 9x + 6",
    "answer": ["-5", "-2", "-1"],
    "variables": {
      "solution_pattern": "common_poly",
      "left_multiplier_str": "x + 8",
      "common_poly_str": "x² + 3x + 2",
      "right_side_coeff": 3,
      "common_poly_roots": [-2, -1]
    }
  },
  {
    "id": "20_polynomial_factorization_a16c1d",
    "task_number": 20,
    "topic": "algebraic_expressions",
    "subtype": "polynomial_factorization",
    "question_text": "Решите уравнение:\nx⁴ = (2x + 3)²",
    "answer": ["-1", "3"],
    "variables": {
      "solution_pattern": "diff_squares",
      "left_side_base_str": "x²",
      "right_side_base_str": "2x + 3",
      "poly_1_coeffs": [1, -2, -3],
      "poly_2_coeffs": [1, 2, 3]
    }
  },
  {
    "id": "20_polynomial_factorization_c5e8a1",
    "task_number": 20,
    "topic": "algebraic_expressions",
    "subtype": "polynomial_factorization",
    "question_text": "Решите уравнение:\nx³ - 2x² - 25x + 50 = 0",
    "answer": ["-5", "2", "5"],
    "variables": {
      "solution_pattern": "grouping",
      "coeffs": [1, -2, -25, 50],
      "group1_multiplier": "x²",
      "group2_multiplier": -25,
      "common_factor_group": "x - 2"
    }
  }
]

🎯 Руководство по Интеграции "Диалоговой Проверки" для Задания 20
📋 Обзор
Данная реализация создаёт полный цикл интерактивной проверки для Задания 20 (polynomial_factorization), строго следуя архитектуре Задания 11 и стандартам ГОСТ.
📁 Структура Файлов
matunya_bot_final/
├── help_core/
│   ├── solvers/
│   │   └── task_20/
│   │       ├── __init__.py
│   │       └── solve_polynomial_factorization.py  ✅ АРТЕФАКТ 1
│   ├── dispatchers/
│   │   └── task_20/
│   │       ├── __init__.py
│   │       └── help_handler_20.py                 ✅ АРТЕФАКТ 2
│   └── prompts/
│       └── task_20_dialog_prompts.py              ✅ АРТЕФАКТ 3
├── handlers/
│   └── callbacks/
│       └── task_handlers/
│           └── task_20/
│               ├── __init__.py
│               └── task_20_handler.py             ✅ АРТЕФАКТ 4
└── utils/
    └── gpt_answer_parser.py                       ✅ АРТЕФАКТ 5
🔧 Шаги Интеграции
Шаг 1: Создание Директорий
bashmkdir -p matunya_bot_final/help_core/solvers/task_20
mkdir -p matunya_bot_final/help_core/dispatchers/task_20
mkdir -p matunya_bot_final/handlers/callbacks/task_handlers/task_20

# Создаём __init__.py файлы
touch matunya_bot_final/help_core/solvers/task_20/__init__.py
touch matunya_bot_final/help_core/dispatchers/task_20/__init__.py
touch matunya_bot_final/handlers/callbacks/task_handlers/task_20/__init__.py
Шаг 2: Размещение Артефактов
Скопируйте содержимое артефактов в соответствующие файлы:

solve_polynomial_factorization.py → help_core/solvers/task_20/
help_handler_20.py → help_core/dispatchers/task_20/
task_20_dialog_prompts.py → help_core/prompts/
task_20_handler.py → handlers/callbacks/task_handlers/task_20/
gpt_answer_parser.py → utils/

Шаг 3: Добавление Состояния
В файл states/states.py добавьте:
pythonclass TaskState(StatesGroup):
    # ... существующие состояния ...
    waiting_task_20_answer = State()  # ← Добавьте эту строку
Шаг 4: Регистрация Router
В главном файле с роутерами (например, main.py или bot.py):
pythonfrom matunya_bot_final.handlers.callbacks.task_handlers.task_20.task_20_handler import router as task_20_router

# В функции main() или где регистрируются роутеры:
dp.include_router(task_20_router)
Шаг 5: Обновление Клавиатур
В файле keyboards/navigation/help_dialog_navigation.py обновите функцию:
pythondef get_help_panel_keyboard(
    task_num: str, 
    subtype: Optional[str] = None, 
    question_num: int = 0
) -> InlineKeyboardMarkup:
    builder = InlineKeyboardBuilder()
    
    # ... существующий код ...
    
    # Добавляем для задания 20
    if task_num == "20":
        builder.button(
            text="❓ Задать вопрос",
            callback_data=TaskCallback(action="20_ask_gpt", task_id=question_num)
        )
        builder.button(
            text="✅ Всё понятно, спасибо!",
            callback_data=TaskCallback(action="close_help", task_id=question_num)
        )
    
    builder.adjust(1)
    return builder.as_markup()
Шаг 6: Интеграция с Real GPT API
В файле utils/gpt_answer_parser.py замените mock-функцию:
pythonasync def parse_math_answer_with_gpt(
    text_answer: str, 
    expected_format: str = "factors"
) -> Optional[Any]:
    """Parse student answer using real GPT."""
    
    logger.info(f"Parsing answer with GPT: {text_answer[:50]}...")
    
    prompt = _build_parsing_prompt(text_answer, expected_format)
    
    try:
        # ЗАМЕНИТЕ на ваш реальный вызов GPT:
        from matunya_bot_final.utils.openai_client import get_gpt_response
        
        response = await get_gpt_response(
            prompt=prompt,
            temperature=0.1,
            max_tokens=200
        )
        
        # Парсим JSON-ответ
        parsed = json.loads(response)
        logger.info(f"Successfully parsed: {parsed}")
        return parsed
        
    except json.JSONDecodeError as exc:
        logger.error(f"Failed to parse JSON: {exc}")
        return None
    except Exception as exc:
        logger.exception(f"GPT parsing failed: {exc}")
        return None
Шаг 7: Настройка task_data
При генерации Задания 20 убедитесь, что task_data содержит:
pythontask_data = {
    "id": "poly_factorization_001",
    "task_type": 20,
    "variables": {
        "polynomial": "x² - 25",  # Исходный многочлен
        "solution_pattern": "diff_squares",  # Метод: common_poly | diff_squares | grouping
        
        # Для common_poly:
        "common_factor": "3x",
        "remaining_expression": "x + 5",
        
        # Для diff_squares:
        "a_term": "x",
        "b_term": "5",
        
        # Для grouping:
        "group1": "x² + 2x",
        "group2": "3x + 6",
        "common_bracket": "x + 2"
    },
    "answer": ["x-5", "x+5"]  # Правильный ответ - список множителей
}
🎬 Пользовательский Сценарий
📝 Вариант 1: Правильный Ответ
1. Бот отправляет задание БЕЗ кнопки "Помощь"
   └─> "Разложите на множители: x² - 25"

2. Ученик пишет: "(x-5)(x+5)"
   └─> GPT парсит текст

3. Сравнение с правильным ответом
   └─> ✅ "Отлично! Правильно!"
❌ Вариант 2: Неправильный Ответ
1. Бот отправляет задание

2. Ученик пишет неправильный ответ

3. Бот: "❌ Не совсем так... Давай разберём правильное решение:"
   └─> Отправляет эталонное решение
   └─> Финальный ответ под <tg-spoiler>

4. Бот: "📚 Сравни свои шаги... 
         Если непонятно — жми ❓ Задать вопрос"
   └─> Появляется клавиатура с кнопками

5. Нажатие на "❓ Задать вопрос":
   └─> GPT: "🤔 На каком шаге у тебя возникли трудности?"

6. Ученик: "Не понял третий шаг"

7. GPT: "Расскажи, что именно ты пытался сделать?"

8. Ученик: "Я вынес икс плюс два"

9. GPT: "Дай проверю: ты вынес <code>(x + 2)</code>? Это так?"
   └─> ВЕРИФИКАЦИЯ!

10. Ученик: "Да"

11. GPT: [Даёт развёрнутый анализ ошибки]
🔍 Ключевые Особенности
✨ Верификация GPT (Критическое Требование)
GPT ОБЯЗАТЕЛЬНО сначала верифицирует понимание записи ученика:
python# В промпте явно прописано:
"""
1. **Верификация понимания:**
   - ОБЯЗАТЕЛЬНО: Когда ученик описывает формулы, 
     СНАЧАЛА переспроси и покажи в <code>...</code>
   - Дождись подтверждения
   - ТОЛЬКО ПОСЛЕ давай развернутый анализ
"""
🔄 Архитектура (Строго по Заданию 11)
Пользователь
    ↓
task_20_handler.py (отправка задания, приём ответа)
    ↓
gpt_answer_parser.py (парсинг текста → машинный формат)
    ↓
Сравнение → Неверно?
    ↓
help_handler_20.py (диспетчер)
    ↓
solve_polynomial_factorization.py (решатель)
    ↓
solution_core (стандарт ГОСТ-2025)
    ↓
_humanize_solution() (HTML + эмодзи)
    ↓
Кнопка "❓ Задать вопрос"
    ↓
task_20_dialog_prompts.py (системный промпт)
    ↓
GPState.in_dialog (диалог с верификацией)
📊 Категории Сообщений

task_messages — основные сообщения задания
help_panels — панели с эталонным решением (НЕ удаляются при диалоге)
dialog_messages — переписка с GPT (удаляются по кнопке "✅ Всё понятно")

🧪 Тестирование
Тест 1: Solver (Математический Мозг)
pythonimport asyncio
from matunya_bot_final.help_core.solvers.task_20.solve_polynomial_factorization import solve

task_data = {
    "id": "test_diff_squares",
    "variables": {
        "polynomial": "x² - 25",
        "solution_pattern": "diff_squares",
        "a_term": "x",
        "b_term": "5"
    },
    "answer": ["x-5", "x+5"]
}

result = asyncio.run(solve(task_data))
print("=== SOLUTION CORE ===")
print(result)

# Ожидаемый вывод:
# {
#   "question_id": "test_diff_squares",
#   "explanation_idea": "Применяем формулу...",
#   "calculation_steps": [...],
#   "final_answer": {"value_display": "(x-5) × (x+5)"},
#   "hints": [...]
# }
Тест 2: Humanizer (Оживитель)
pythonfrom matunya_bot_final.help_core.dispatchers.task_20.help_handler_20 import get_task_20_help

help_text, solution_core = asyncio.run(get_task_20_help(task_data))

print("=== HUMANIZED TEXT ===")
print(help_text)

# Ожидаемый вывод: HTML с эмодзи, шагами и <tg-spoiler>
Тест 3: GPT Parser
pythonfrom matunya_bot_final.utils.gpt_answer_parser import parse_math_answer_with_gpt

test_answers = [
    "(x-5)(x+5)",
    "икс минус пять умножить на икс плюс пять",
    "3(x+1)(x-1)",
    "минус пять; два; пять"  # для формата "numbers"
]

for answer in test_answers:
    parsed = asyncio.run(parse_math_answer_with_gpt(answer, "factors"))
    print(f"Ответ: {answer}")
    print(f"Распознано: {parsed}\n")
Тест 4: Полный Цикл (End-to-End)
python# 1. Создать task_data
# 2. Сохранить в state: await state.update_data(task_20_data=task_data)
# 3. Отправить задание через callback "20_send_task"
# 4. Написать неправильный ответ
# 5. Проверить, что появилось эталонное решение
# 6. Нажать "❓ Задать вопрос"
# 7. Написать вопрос
# 8. Проверить верификацию GPT
⚠️ Важные Замечания
🚨 Обязательные Действия

Замените mock GPT в gpt_answer_parser.py на реальный API
Добавьте состояние waiting_task_20_answer в states.py
Зарегистрируйте router в главном файле
Обновите клавиатуры с кнопками для задания 20
Проверьте структуру task_data при генерации заданий

💡 Рекомендации

Логируйте все критические операции через logger
Оборачивайте async-вызовы в try-except
Используйте message_manager для отслеживания сообщений
Тестируйте каждый компонент отдельно перед интеграцией

🔒 Безопасность

Валидируйте task_data перед использованием
Проверяйте наличие обязательных полей
Обрабатывайте случаи, когда GPT не может распознать ответ
Логируйте ошибки, но не показывайте их пользователю

📚 Соответствие Стандартам
✅ ГОСТ-РЕШАТЕЛЬ-2026

 Файл в help_core/solvers/task_20/
 Функция async def solve(task_data: dict) -> dict
 Возвращает полный solution_core
 Все ключи присутствуют (даже заглушки)

✅ ГОСТ-2025 (solution_core)

 question_id — уникальный ID
 question_group — группа задания
 explanation_idea — суть метода
 calculation_steps — список шагов
 final_answer — итоговый результат
 hints — список подсказок

✅ Архитектура Задания 11

 Solver → Dispatcher → Humanizer
 Динамический импорт через importlib
 Категории сообщений (help_panels, dialog_messages)
 GPT диалог через GPState.in_dialog
 Системный промпт с верификацией

✅ Чеклист Готовности
Файловая Структура

 Созданы все директории
 Размещены 5 артефактов
 Добавлены __init__.py файлы

Код и Интеграция

 Состояние waiting_task_20_answer добавлено
 Router зарегистрирован в main
 Клавиатуры обновлены с кнопками для #20
 Mock GPT заменён на реальный API

Данные

 task_data содержит все нужные поля
 variables включает solution_pattern
 answer корректно заполнен

Тестирование

 Solver работает и возвращает solution_core
 Humanizer форматирует текст с эмодзи
 Parser распознаёт разные форматы ответов
 Полный цикл проверен End-to-End

Логирование и Обработка Ошибок

 Все async-функции с try-except
 Критические операции логируются
 Пользователь видит понятные сообщения об ошибках

🎉 Готово!
После выполнения всех шагов система "Диалоговой Проверки" для Задания 20 будет полностью интегрирована и готова к работе!

Версия документа: 1.0
Дата: 2025
Стандарты: ГОСТ-2025, ГОСТ-РЕШАТЕЛЬ-2025
Эталон: Задание 11 (form_match_mixed)


UI-макет: Идеальное Окно "Помощи" (Задание 20)
(Сообщение от бота, желательно скрытое под <tg-spoiler>)
💡 Идея решения
Это уравнение решается методом группировки. Сначала сгруппируем слагаемые, затем вынесем общие множители, чтобы получить произведение скобок, равное нулю.
🔎 Пошаговое решение
Шаг 1: Сгруппируем первые два и последние два слагаемых.
<code>(3x³ - 15x²) + (-12x + 60) = 0</code>
Шаг 2: Вынесем общий множитель из каждой группы: 3x² из первой и -12 из второй.
<code>3x²(x - 5) - 12(x - 5) = 0</code>
Шаг 3: Теперь вынесем общую скобку (x - 5) за скобку.
<code>(x - 5)(3x² - 12) = 0</code>
Шаг 4: Заметим, что из второй скобки можно вынести множитель 3.
<code>3(x - 5)(x² - 4) = 0</code>
Шаг 5: Произведение равно нулю, когда один из множителей равен нулю. Получаем два уравнения:
<code>x - 5 = 0</code> или <code>x² - 4 = 0</code>
Шаг 6: Решим квадратное уравнение x² - 4 = 0. Коэффициенты: a = 1, b = 0, c = -4.
Шаг 7: Найдем дискриминант по формуле D = b² - 4ac:
<code>D = (0)² - 4 · (1) · (-4) = 16</code>
Шаг 8: Найдем корни по формуле x = (-b ± √D) / 2a:
<code>x = (-(0) ± √16) / (2 · 1)</code>
Корни: x₁ = 2, x₂ = -2
Шаг 9: Решим линейное уравнение x - 5 = 0.
Корень: x₃ = 5
✅ Ответ
-2; 2; 5
💭 Полезно помнить
• Метод группировки работает, когда отношение коэффициентов первой и второй пары слагаемых равно.
• Не забывайте проверять, можно ли дальше упростить выражения в скобках (как мы сделали с x² - 4).


!!!Технический долГ!!! (опционально)
Можно перейти на matunya_bot_final\docs\help_core\spec_solution_core_v2_3.md
Отлично, Оленька 🌸 — ты прямо в точку смотришь в код.
Да, вот по этому файлу можно сказать абсолютно точно:
задание 20 (подтип polynomial_factorization) полностью работает по типовому стандарту v2.2 (ГОСТ-2026).

🔍 Почему это видно из кода

Посмотри на этот фрагмент:

solution_core = {
    "question_id": ...,
    "question_group": "POLYNOMIAL_FACTORIZATION",
    "explanation_idea": explanation,
    "calculation_steps": steps,
    "final_answer": {...},
    "validation_code": None,
    "hints": hints,
}


— здесь только поля v2.2:
explanation_idea, calculation_steps, final_answer, hints, validation_code.

👉 Никаких steps, final_block, remember_block, meta и т. д. — а значит, стандарт v2.3 не используется.

⚙️ Что это значит

Решатель ( solver ) выдаёт структуру v2.2.

Humanizer для задания 20 читает calculation_steps и выводит пошаговое объяснение (как на твоём скриншоте).

Вся цепочка «solver → humanizer → dispatcher» остаётся старого типа — но работает идеально.

💡 Если захочешь перевести 20-е задание на v2.3

Можно сделать плавный апгрейд, не ломая старое:

Добавить в solution_core блоки
steps, final_block, remember_block, meta — по примеру задания 6.

Оставить старые поля (calculation_steps, final_answer, hints) для обратной совместимости.

Обновить humanizer, чтобы он при наличии steps использовал новый формат.

Так 20-е задание будет выглядеть единообразно с 6-м (💡 Идея, 🔎 Шаги, ✅ Ответ, 💭 Что запомнить).

⚖️ Сравнение стандартов solution_core
Блок	v2.2 (ГОСТ-2026, текущий у задания 20)	v2.3 (расширенный, как у задания 6)	Зачем добавлено
question_id / question_group	✅ есть	✅ есть	Уникальный идентификатор и группа подтипов
explanation_idea	✅ есть	✅ есть	Краткая идея решения (остаётся)
calculation_steps	✅ основной блок	⚙️ сохраняется для совместимости	Старый формат шагов, текстовый
steps	❌ нет	✅ новый основной блок	Массив структур: title, explanation, expression, result, hints — позволяет красиво форматировать формулы и подшаги
final_answer	✅ есть	⚙️ сохраняется как старое поле	Итог, выводимый в упрощённом виде
final_block	❌ нет	✅ новый блок	Расширенный ответ: summary, primary_value (fraction/decimal), alternatives, checks
remember_block	❌ нет	✅ новый блок	Памятка: советы, типичные ошибки, теория — выводится в конце с 💭 или спойлером
answer	❌ нет	✅	Машинный формат ответа (числа, флаги точности)
meta	❌ нет	✅	Служебные данные (тип задачи, сложность, источник выражения)
hints	✅ есть	⚙️ дублируется из remember_block["tips"]	Старый список советов — сохраняется для обратной совместимости
validation_code	✅ есть	✅ есть	Код проверки ответа (обычно Python)
💎 Что даёт переход на v2.3 для задания 20
Возможность	Старый стандарт	Новый стандарт
Формулы с HTML-оформлением	ограничено	✅ можно использовать <b>, <code>, <tg-spoiler>
Вложенные шаги и пояснения	текстовые блоки	✅ структурированные steps[]
Финальный ответ (дробь, период, степень)	только строка	✅ детализировано в final_block.primary_value
Памятка и теория	через hints	✅ отдельный remember_block
Совместимость со старыми humanizer’ами	полная	✅ сохраняется (dual-mode)
📘 Что делать с заданием 20
Шаг	Действие	Пример
1	Добавить в solution_core новые поля steps, final_block, remember_block, meta	можно брать структуру из задания 6
2	Заполнять их из существующих данных (calculation_steps, hints)	автоматическое преобразование шагов
3	Проверить, что humanizer корректно читает и новый, и старый формат	при наличии steps отдаёт HTML как у 6-го задания
4	Обновить help_core/humanizers/template_humanizers/task_20_humanizer.py	чтобы поддерживал оба варианта
