Документ: "Архитектура и Реализация Задания №11"
Назначение: Полное техническое и логическое описание всех компонентов, отвечающих за генерацию, валидацию и отрисовку Задания №11.
Расположение: matunya_bot_final/task_generators/task_11/README.md
(Содержимое документа)
code
Markdown
# Архитектура и Реализация Задания №11

## 1. Общая Концепция

Задание №11 в ОГЭ — это задача на сопоставление (matching), где ученику необходимо установить соответствие между тремя графиками (А, Б, В) и тремя вариантами (формулами или свойствами).

Ключевой принцип реализации — **модульность и унификация**. Каждый подтип задания реализуется парой "Генератор + Валидатор", а все общие инструменты (как отрисовка графиков) вынесены в отдельные утилиты.

## 2. Структура Папок
task_generators/task_11/
├── init.py
├── generators/
│ ├── init.py
│ ├── match_signs_a_c_generator.py # Подтип "Знаки a и c"
│ └── form_match_mixed_generator.py # Подтип "Смешанные функции"
├── validators/
│ ├── init.py
│ ├── match_signs_a_c_validator.py
│ └── form_match_mixed_validator.py
├── formula_generators.py # "Мозг" для создания формул
└── README.md # Этот документ
code
Code
---

## 3. Подтипы и их Различия

На данный момент реализовано два ключевых подтипа, которые имеют **разную логику генерации**, но приводятся к **единой структуре данных**.

### 3.1. Подтип `match_signs_a_c`

*   **Суть:** Сопоставить графики парабол с комбинациями знаков коэффициентов `a` и `c`.
*   **Логика Генератора:**
    1.  Генерирует 3 набора коэффициентов `(a, b, c)` так, чтобы пары знаков `(a, c)` были **уникальны**.
    2.  Из 4-х возможных комбинаций знаков случайным образом выбираются 3 для отображения.
    3.  **Не использует** `formula_generators.py`, так как ему не нужны конкретные строковые формулы, а только сами коэффициенты.
    4.  Для каждого графика вызывает "Художника" (`plot_generator`), передавая ему **словарь с коэффициентами** (`coeffs`).

### 3.2. Подтип `form_match_mixed`

*   **Суть:** Сопоставить графики разных типов функций (парабола, гипербола, прямая, корень) с их формулами.
*   **Логика Генератора:**
    1.  Случайным образом выбирает "сценарий" — комбинацию из 3-х разных типов функций.
    2.  Обращается к **`formula_generators.py`**, чтобы получить 3 готовых "пакета" с лямбда-функцией, строковым представлением и типом для каждого графика.
    3.  Для каждого графика вызывает "Художника" (`plot_generator`), передавая ему **лямбда-функцию** (`func`).

---

## 4. Общие Компоненты и Стандарты

### 4.1. "Мозг" — `formula_generators.py`

*   Это централизованная библиотека, которая умеет генерировать **случайные, но адекватные** формулы и лямбда-функции для всех базовых типов графиков.
*   Каждая функция в этом модуле возвращает стандартизированный словарь, содержащий `func`, `formula_str`, `type` и `coeffs` (для парабол).
*   **Принцип:** Генераторы заданий не "придумывают" формулы сами, а запрашивают их у этого модуля.

### 4.2. "Художник" — `utils/visuals/plot_generator.py`

*   Это универсальный инструмент для отрисовки графиков.
*   Он умеет принимать на вход данные в **разных форматах**:
    *   Словарь с **коэффициентами** (`coeffs`) для парабол.
    *   Словарь с **лямбда-функцией** (`func`) для остальных типов.
*   Он отвечает за создание красивых, стандартизированных изображений в стиле "Феникс".

### 4.3. "Золотой Стандарт" Валидаторов — ГОСТ-ВАЛИДАТОР-2025

*   **Все валидаторы** в этой директории (`validators/`) **обязаны** соответствовать "ГОСТ-ВАЛИДАТОР-2025".
*   **Формат возврата:** `Tuple[bool, List[str]]`.
    *   `True, []` — в случае успеха.
    *   `False, ["Текст ошибки 1", "Текст ошибки 2"]` — в случае провала.

### 4.4. Стандарт Структуры `task_data`

*   Каждый генератор на выходе формирует словарь (`task_data`) с единой структурой, содержащей `id`, `task_type`, `subtype`, `text`, `answer`, `source_plot` и т.д.
*   **Ключевой принцип:** Данные, передаваемые на отрисовку или сохранение в БД, должны быть `JSON`-сериализуемыми. **Никаких "сырых" лямбда-функций** в итоговом словаре быть не должно.

---

## 5. Процесс Наполнения Базы (`populate`)

*   Скрипт `scripts/populate/populate_task_11_db.py` является "сборщиком" для Задания 11.
*   Он последовательно вызывает **все** генераторы из `generators/`.
*   Для каждой сгенерированной задачи он вызывает соответствующий валидатор.
*   Он ожидает от валидаторов **только** формат `Tuple[bool, List[str]]`.
*   Успешно сгенерированные и провалидированные задачи сохраняются в `data/tasks_11/tasks_11.json`.

Промпт для консилиума Золотой Набор

Привет!

Ты участвуешь в консилиуме ИИ по созданию "Золотых Наборов" — лучших объяснений и метафор для Telegram-бота "Матюня". "Матюня" — дружелюбный репетитор, который помогает школьникам готовиться к ОГЭ по математике.

Сейчас ты — опытный педагог-методист и креативный автор. Ты умеешь объяснять сложные вещи просто, ярко и с юмором, чтобы подростки 15-17 лет их запоминали.

🎯 Твоя задача: Для каждого указанного подтипа задания придумать 5-7 уникальных, ярких и полезных фраз-подсказок. Это не должны быть полноценные карточки, а именно "россыпь жемчуга" — короткие, запоминающиеся идеи, метафоры и алгоритмы.

📦 Требования к каждой фразе:

Стиль: В духе "Матюни" — дружелюбный, понятный, поддерживающий. Используй метафоры, сравнения, юмор, жизненные аналогии.

Цель: Фраза должна помогать ученику понять ключевую идею для решения подтипа.

Формат: Просто текст. Никаких заголовков, примеров или спойлеров.

⚠️ Важные правила:

Не повторяйся. Каждая фраза должна нести новую мысль или метафору.

Пиши просто, без заумных терминов.

Запрещено использовать LaTeX и другие технические обозначения. Используй обычные символы: y = kx + b, √x, x², 3/4.

🎯 Цель консилиума: Собрать богатую "палитру стиля" для каждого подтипа. Позже эти фразы будут загружены в Google-таблицу и станут основой для ответов GPT в режиме Помощи.

Задание 11:

Подтип: match_signs_k_b (Определение знаков k и b для линейной функции y = kx + b по графику).

Сгенерируй 5-7 фраз с объяснением для этого подтипа.
