# 📘 Задание 11 — Соответствие графиков и коэффициентов параболы

📘 Документация по заданию №11 (ОГЭ-2026)
🔑 Контекст

Задание №11 в ОГЭ проверяет умение работать с графиками функций.
Мы договорились делать подтипы (subtypes), чтобы покрыть все разновидности заданий из ОГЭ-2026.

📌 Полный список подтипов (8 штук)

match_signs_a_c
Установить соответствие между графиком квадратичной функции и знаками коэффициентов a и c.

match_signs_k_b
Установить соответствие между графиком линейной функции и знаками коэффициентов k и b.

form_match_linear
Узнавание линейной функции по её формуле.

form_match_linear_constant
Узнавание линейной функции при особых случаях (например, y = kx, y = b).

form_match_parabola
Узнавание параболы по её формуле.

form_match_root
Узнавание графика функции вида √x + c.

form_match_hyperbola
Узнавание гиперболы по её формуле.

form_match_mixed
Сочный подтип: даётся 3 графика разных типов (линейная, парабола, гипербола, корень) + 1 отвлекающая формула.

⚙️ Архитектура реализации
📂 Структура папок
matunya_bot_final/
 └── task_generators/
      └── task_11/
           ├── generators/
           │     ├── match_signs_a_c_generator.py
           │     ├── form_match_mixed_generator.py
           │     └── ...
           ├── validators/
           │     ├── match_signs_a_c_validator.py
           │     ├── form_match_mixed_validator.py
           │     └── ...
           ├── __init__.py
           └── formula_generators.py

🧩 Разделение обязанностей

Генераторы создают задачу в виде JSON (без картинок).

Формульные генераторы (formula_generators.py) содержат вспомогательные функции: генерация линейных, парабол, гипербол, корней.

Валидаторы проверяют результат генератора (правильный формат, уникальные графики, корректные формулы).

Рисовальщик (create_graph) создаёт PNG-графики на основе func_data.

populate_db.py собирает все задачи в одну JSON-базу (tasks_11.json).

🧪 Тесты

Для каждого подтипа создаётся свой тестовый файл:

tests/task_generators/task_11/test_match_signs_a_c.py
tests/task_generators/task_11/test_form_match_mixed.py
...

Что тестируем:

Генератор возвращает корректный JSON.

Валидатор не выдаёт ошибок (errors == []).

Многократные прогоны (10–50 раз) → устойчивость.

✅ Наши договорённости

Один JSON для всей БД заданий №11.

Генераторов и валидаторов — много (по подтипам).

Все подтипы подключаются через __init__.py в GENERATOR_MAP и VALIDATOR_MAP.
"""
📌 Особенности архитектуры:
- Генераторы:
  Все генераторы подтипов регистрируются в общем словаре GENERATOR_MAP
  внутри пакета task_11/generators. Наружу мы экспортируем только:
    • generate_task_11_by_subtype(subtype) — универсальный вызов генератора
    • GENERATOR_MAP — словарь {subtype: generator_function}
  👉 Отдельные функции генераторов (например, generate_task_11_match_signs_a_c)
     не импортируются сюда напрямую.

- Валидаторы:
  Для каждого подтипа свой валидатор, и их мы подключаем вручную здесь.
  Универсального диспетчера пока нет. Поэтому новые подтипы добавляются
  через явный импорт и регистрацию в __all__.

Таким образом:
- Генераторы подключаются через GENERATOR_MAP.
- Валидаторы — явным импортом по каждому подтипу.
"""

Рисовальщик вызывается отдельно (не внутри генератора).

У каждого подтипа свой генератор + валидатор + тест.

Начинаем с match_signs_a_c и form_match_mixed (уже реализованы).

После них берём простые: match_signs_k_b, form_match_linear, затем остальные.

Возвращаемый JSON
{
  "id": "11_match_signs_a_c_ab12cd",
  "task_id": "11_05",
  "task_type": 11,
  "subtype": "match_signs_a_c",
  "category": "matching",
  "subcategory": "coefficients",
  "text": "... условие задачи ...",
  "answer": ["1", "2", "3"],
  "func_data": [ ... ],    // список функций с коэффициентами
  "x_lim": [-5, 5],
  "y_lim": [-5, 5],
  "source_plot": {
    "plot_id": "match_signs_a_c",
    "params": {
      "labels": ["A", "Б", "В"],
      "graphs": ["...png", "...png", "...png"],
      "options": {
        "1": "a > 0, c > 0",
        "2": "a > 0, c < 0",
        "3": "a < 0, c > 0",
        "4": "a < 0, c < 0"
      }
    }
  }
}

4. 🖼️ Рисовальщик графиков (utils/visuals/plot_generator.py)
Особенности

Использует matplotlib + seaborn.

Поддерживает разрывы (например, гиперболы).

Красивое оформление:

стрелки на осях,

подписи x, y у осей,

точка O в начале координат,

убраны метки ±1 на шкале,

сетка и стили как в экзаменационных материалах.

Вывод

PNG-файлы сохраняются в temp/task_11/....
Пример: temp/task_11/parabola_A_a1c2.png.



ТЕСТЫ
Только задание №11
pytest -v tests/task_generators/task_11/

🎯 Отдельный подтип

Например, только form_match_mixed:

python -m matunya_bot_final.scripts.populate.populate_task_11_db --count 50


Или только match_signs_a_c:

pytest -v tests/task_generators/task_11/test_match_signs_a_c.py

🔁 Многократный прогон (например, 100 раз)
pytest -v tests/task_generators/task_11/test_form_match_mixed.py --count=100

запуск скрипта для БД
python -m scripts.populate.populate_task_11_db

Очистить и заново наполнить (полезно после обновления генератора):
python -m scripts.populate.populate_task_11_db --clear -n 5


✅ Запуск тестов только для задания 11
(.venv) C:\Users\user\Documents\matunya>pytest -v matunya_bot_final/tests/task_generators/task_11

✅ Запуск конкретного теста (например, для form_match_mixed)
(.venv) C:\Users\user\Documents\matunya>pytest -v matunya_bot_final/tests/task_generators/task_11/test_form_match_mixed.py

🧹 Полный прогон всех тестов проекта
(.venv) C:\Users\user\Documents\matunya>pytest -v

🚀 Запуск скрипта наполнения БД (populate)

Из корня проекта (C:\Users\user\Documents\matunya):

(.venv) C:\Users\user\Documents\matunya>python -m matunya_bot_final.scripts.populate.populate_task_11_db --n 10 --clear


✅ Теперь можно запускать так:

Сгенерировать по 20 задач:

python -m matunya_bot_final.scripts.populate.populate_task_11_db --n 20


Очистить JSON и сгенерировать по 10 задач:

python -m matunya_bot_final.scripts.populate.populate_task_11_db --n 10 --clear




При будущих запусках populate-скриптов не забывать выставлять PYTHONPATH=c:\Users\user\Documents\matunya;c:\Users\user\Documents\matunya\seaborn и PYTHONIOENCODING=utf-8, чтобы избежать проблем с импортами и выводом.

Новый запуск
C:\Users\user\Documents\matunya> .venv\Scripts\activate
(.venv) C:\Users\user\Documents\matunya> set PYTHONPATH=C:\Users\user\Documents\matunya && set PYTHONIOENCODING=utf-8 && python -m matunya_bot_final.scripts.populate.populate_task_11_db --count 50 (наполнение 50 задачами)

Запуск теста
(.venv) C:\Users\user\Documents\matunya> set PYTHONPATH=C:\Users\user\Documents\matunya && set PYTHONIOENCODING=utf-8 && pytest -v matunya_bot_final/tests/task_generators/task_11
