Проблема с жирными числами в задании 11
Описание проблемы
При попытке реализовать жирные числа в задании 11 типа (квадратичные функции) возникает ошибка Telegram API:
TelegramBadRequest: Telegram server says - Bad Request: can't parse entities: 
Unsupported start tag "" at byte offset 271/306
Контекст системы
В проекте существует системное правило: все числа должны быть жирным начертанием.
Для этого используется:

Функция bold_numbers() в utils/text_formatters.py
Глобальная настройка parse_mode="HTML" в системе

Техническая детализация
Что работает

Обычный текст без форматирования: parse_mode=None работает корректно
Другие задания: функция bold_numbers() работает в остальных частях системы
Изображения: отправка графиков работает без проблем

Что не работает

HTML форматирование в задании 11: любое использование parse_mode="HTML" вызывает ошибку
Функция bold_numbers(): создает HTML, который Telegram не может распарсить для этого конкретного текста

Отладочные данные
Исходный текст (длина 292 символа):
'На рисунках изображены графики квадратичных функций y = ax² + bx + c.
Установи соответствие между графиками и знаками коэффициентов a и c.

1) a > 0, c > 0
2) a > 0, c < 0  
3) a < 0, c > 0
4) a < 0, c < 0

Ответ:  А ___   Б ___   В ___

👉 В ответе укажи соответствующий номер (например: 3 2 1)'
Ошибка возникает на позиции 271/306 - примерно в районе слова "номер" в конце текста.
Рабочее решение (временное)
pythonawait bot.send_message(
    chat_id=query.message.chat.id,
    text=clean_text,  # обычный текст без HTML
    parse_mode=None,  # явно отключаем HTML парсинг
    reply_markup=get_task_11_keyboard()
)
Возможные причины проблемы
1. Специальные символы
В тексте задания есть символы, которые могут конфликтовать с HTML:

Математические символы: ax², bx, >/<
Эмодзи: 👉
Подчеркивания: ___

2. Конфликт с регулярным выражением
Функция bold_numbers() использует regex:
pythonre.sub(r'(\d+[\.,]?\d*)', r'<b>\1</b>', text)
Возможно, этот паттерн создает некорректный HTML для данного конкретного текста.
3. Длина текста
Текст задания 11 длиннее обычных (292 символа), что может влиять на парсинг HTML.
Варианты решений
Вариант 1: Unicode жирные цифры
pythonformatted_text = text.replace("1)", "𝟏)").replace("2)", "𝟐)").replace("3)", "𝟑)").replace("4)", "𝟒)")
Плюсы: Работает без HTML парсинга
Минусы: Не все устройства поддерживают Unicode математические символы
Вариант 2: Кастомная функция для задания 11
pythondef bold_numbers_task_11(text):
    # Более точное регулярное выражение специально для задания 11
    text = re.sub(r'^(\d+)\)', r'<b>\1</b>)', text, flags=re.MULTILINE)  # только номера пунктов
    text = re.sub(r'\b(\d+)\b', r'<b>\1</b>', text)  # остальные числа
    return text
Вариант 3: Markdown вместо HTML
pythonawait bot.send_message(
    chat_id=query.message.chat.id,
    text=text.replace("1)", "*1)*").replace("2)", "*2)*"),  # и т.д.
    parse_mode="Markdown"
)
Вариант 4: Разбиение текста
Отправлять длинный текст частями, применяя форматирование к каждой части отдельно.
Вариант 5: Отладка функции bold_numbers()
Добавить логирование в utils/text_formatters.py чтобы увидеть, какой именно HTML создается:
pythondef bold_numbers(text: str) -> str:
    result = re.sub(r'(\d+[\.,]?\d*)', r'<b>\1</b>', text)
    print(f"DEBUG bold_numbers: {repr(result[:100])}")  # первые 100 символов
    return result
Исследовательские задачи

Протестировать каждый вариант на задании 11
Найти точную причину ошибки - какие символы/комбинации вызывают проблему
Проверить совместимость с остальной системой
Убедиться в кроссплатформенности решения

Приоритеты

Высший: Решение должно работать стабильно
Высокий: Соответствие системному правилу (жирные числа)
Средний: Простота реализации
Низкий: Совместимость со старыми устройствами

Текущий статус
Статус: Временно решено (без жирных чисел)
Требуется: Полное решение с сохранением форматирования
Ответственный: Разработчик системы форматирования
Дедлайн: По возможности