🧭 Как переключаться между ветками
1. Посмотреть список всех веток
git branch


🔹 Ветка, на которой ты сейчас, будет отмечена звёздочкой *.
Пример:

* feat/help-system-dev
  main

2. Переключиться обратно на основную ветку (рабочую)
git checkout main

3. Вернуться в тестовую ветку (где работает Кодекс)
git checkout feat/help-system-dev

👀 Как посмотреть, что в ветке
1. Проверить, какие изменения есть
git status


Покажет новые, изменённые и неотслеживаемые файлы.

2. Сравнить с основной веткой (например, увидеть, что именно поменялось)
git diff main

3. Открыть визуально в VS Code

VS Code умеет показывать ветки прямо внизу:

посмотри в левом нижнем углу — там написано, например:
feat/help-system-dev
(если ты в этой ветке).

если кликнуть по названию — появится выпадающий список всех веток, и ты можешь выбрать любую.

💚 Важно: при переключении ветки Git меняет файлы в проекте на те, что сохранены именно в этой ветке.
То есть VS Code автоматически покажет нужную версию файлов — ты всегда видишь актуальное состояние ветки.


🔒 3. Пока Кодекс работает:

Ты можешь спокойно смотреть, какие файлы он меняет:

в Source Control (вкладка с иконкой ответвления 🪴)

или через терминал:

git status


🧠 Что произойдёт, если что-то пойдёт не так

Если вдруг Кодекс накосячит (ну или ты захочешь откатить его изменения),
достаточно одной команды:

git checkout main

🎯 — и ты мгновенно возвращаешься в свой старый, стабильный Матюня,
в котором всё работает ровно так, как было.

💚 Что можно делать без страха

смотреть, как Кодекс добавляет блоки;

запускать Матюню локально;

править тексты, шаблоны, даже удалять куски кода —
всё это не тронет твою основную ветку.

🚀 Когда ты решишь, что всё готово

Тогда мы просто:

проверим, что код в feat/help-system-dev работает идеально;

аккуратно «польём» его в main (через merge или pull request);

и Матюня получит обновление с новым блоком Частые ошибки.

Вот как обстоят дела сейчас:

🔹 main — это твоя “золотая копия” Матюни.
🔹 feat/help-system-dev — свежая ветка, созданная от неё без изменений.

📖 Проще говоря:

feat/help-system-dev = полная копия main
(ни одной строчки не отличается, пока Кодекс не начнёт что-то добавлять)

💡 Это значит:

если сейчас запустить Матюню — он будет работать точно как раньше;

ты можешь спокойно экспериментировать, Кодекс может писать код,
а твоя «основная» версия остаётся вне опасности;

в любой момент можно вернуться в main одной командой:

git checkout main

git checkout feat/help-system-dev


чтобы быстро прыгать туда-сюда между двумя последними ветками, можно использовать короткую форму:
git checkout -

(тире в конце — Git запомнит последнюю ветку и вернёт тебя туда)


⚙️ Как делается коммит (по шагам)

1️⃣ Отметить, какие файлы надо сохранить:

git add docs


(или git add ., если хочешь добавить всё сразу)

2️⃣ Сделать снимок с комментарием:

git commit -m "update: обновила материалы в docs"


💡 После этого Git говорит:

«Окей, я запомнил это состояние в истории ветки.»

Проверим, есть ли там папка docs:
dir matunya_bot_final /ad


💡 Как проверить один конкретный файл
git diff matunya_bot_final/docs/Git.md

Когда ты потом захочешь «добавить эти изменения в стабильную версию»,
мы просто сделаем:

git checkout main
git merge feat/help-system-dev

💾 Шаги для коммита:

1️⃣ Проверяешь, есть ли изменения:

git status


2️⃣ Добавляешь в зону сохранения (можно указать конкретный файл, либо всё сразу):

git add .


3️⃣ Делаешь коммит — то есть сохраняешь снимок в истории ветки:

git commit -m "update: кратко опиши, что изменила" --no-verify

💡 Пример по-человечески:

я изменила Git.md в docs, тогда пишу так 👇

git add matunya_bot_final/docs/Git.md
git commit -m "update: добавила новый раздел в Git.md" --no-verify


После этого Git ответит чем-то вроде:

[feat/help-system-dev 123abc4] update: добавила новый раздел в Git.md
 1 file changed, 12 insertions(+)


🎉 Это значит — изменения сохранены именно в ветке feat/help-system-dev,
и main при этом осталась нетронутой.

Проверка коммита. 🌸
Тест изменения 🌸
