## 🧩 Golden Set (GPT-диалоги) — отложенная задача

### 📌 Что такое Golden Set
Golden Set — это набор заранее подготовленных фраз (метафоры, сравнения, стиль Матюни),
которые подгружаются из Google Sheets и передаются в GPT как часть system prompt.

Цель:
- улучшить стиль ответов GPT,
- сделать объяснения более «матюньскими»,
- снизить риск сухих или неудачных формулировок.

На данный момент Golden Set **полноценно используется только в задании №11** (эксперимент).

---

### ⚠️ Текущая проблема (январь 2026)

При запуске GPT-диалога для задания №16 наблюдается:
- задержка ответа GPT;
- иногда ответ приходит только после нажатия «Закрыть помощь».

В логах:
- ошибка Google Sheets `503 Service Unavailable`;
- ожидание ответа от `get_golden_set(...)`;
- GPT вызывается **после** этой задержки.

Причина:
- для задания №16 **нет таблицы Golden Set**;
- функция `get_golden_set` всё равно пытается подключиться к Google Sheets;
- при ошибке Google происходит блокировка диалогового контекста.

---

### 🧠 ВАЖНОЕ архитектурное понимание (зафиксировано)

- ❌ проблема **НЕ** в:
  - `common.py`
  - `help_handler.py`
  - `solver`
  - `humanizer`

- ✅ Golden Set — это часть **GPT-диалога**, а не решения задачи.

- ✅ Единственное корректное место для Golden Set:
matunya_bot_final/help_core/dialog_contexts/


---

### 🎯 КЛЮЧЕВОЙ ФАЙЛ (ответственный)

matunya_bot_final/help_core/dialog_contexts/task_16_context.py


Именно здесь происходит подключение Golden Set:

```python
from matunya_bot_final.help_core.knowledge.golden_set_reader import get_golden_set

...

golden_set = await get_golden_set(pattern, task_type=16)
Далее golden_set передаётся в:

get_task_16_dialog_prompt(...)
🧩 Почему сейчас НЕ правим
Golden Set концептуально важен, но:

для задания 16 таблицы ещё нет;

Google Sheets нестабилен;

правки в common или хендлерах сломают архитектуру;

задание 11 уже корректно зависит от Golden Set.

Принято решение:

НЕ ломать существующую систему ради временной проблемы.

🛠 План на будущее (варианты решения)
Рассмотреть один из вариантов:

Мягкий fallback

get_golden_set возвращает [] мгновенно при ошибке

без ожидания Google

Ограничение по заданиям

Golden Set включается ТОЛЬКО для заданий, где есть таблица (например, №11)

Timeout / async fallback

если Google не ответил за N мс — продолжаем без Golden Set

Предзагрузка / кеш

кешировать Golden Set заранее, не в момент запуска диалога

✅ Текущий статус
Golden Set оставлен подключённым в task_16_context.py

Ошибка признана архитектурной, а не багом

Решение осознанно отложено

Работа по заданию №16 продолжается дальше
