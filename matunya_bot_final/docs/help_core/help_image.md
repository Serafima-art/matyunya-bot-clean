📄 Контракт help_image в solution_core
📌 Назначение

Поле help_image предназначено для синхронного отображения:

визуальной схемы (картинки),

и её текстового описания,
используемого GPT при формировании объяснений.

Контракт гарантирует, что:

GPT не запрашивает у ученика описание рисунка;

текстовое объяснение всегда согласовано с отображаемой картинкой;

логика помощи масштабируется на множество изображений.

📦 Где используется

Формируется исключительно в solver’е

Передаётся:

в handler → для отображения картинки

в GPT-контекст → как факт задачи

🧱 Структура solution_core
    solution_core: Dict[str, Any] = {
        ...
        "help_image": {
        "file": str,
        "schema": str,
        "params": Dict[str, Any],
    }
}

🧩 Поля контракта
help_image.file ✅ (обязательное)
Имя файла изображения (без пути)


Тип: str
Пример:

"help_cyclic_quad_angles_abcd_diags_arc_ac.png"


📌 Требования:

файл обязан существовать в директории:

matunya_bot_final/non_generators/task_<N>/assets/


путь к файлу не хранится в контракте

расширение указывается явно (.png, .jpg)

help_image.description (НЕ хранится в solution_core)
Формируется в dialog_context на основе schema + params
и передаётся GPT как факт задачи

Тип: str

📌 Требования:

описывает геометрический факт, а не внешний вид

не содержит вопросов

не обращается к ученику

не просит показать / описать рисунок

формулируется так, будто GPT “знает рисунок”

✅ Правильно:

На рисунке изображён вписанный четырёхугольник. 
Два отмеченных угла опираются на одну и ту же дугу окружности.


❌ Неправильно:

Посмотри на рисунок и попробуй определить, на какую дугу опираются углы.

help_image.key ⛔ (опционально, рекомендовано)
Семантический ключ описания


Тип: str
Пример:

"same_arc"


📌 Используется:

для выбора шаблонного описания

для унификации логики между паттернами

не обязателен, но рекомендуется для масштабирования

🔒 Архитектурные ограничения (ОБЯЗАТЕЛЬНО)
❌ Запрещено

Формировать help_image в handler’е

Формировать help_image в humanizer’е

Генерировать описание картинки через GPT

Просить ученика описывать или показывать рисунок

Делать описание зависимым от букв (A, B, C, D), если это не критично

✅ Разрешено

Использовать шаблонные описания, общие для группы изображений

Использовать одинаковое описание для разных файлов, если геометрическая идея совпадает

Менять файл картинки без изменения текста GPT, если сохранён смысл

🔁 Жизненный цикл help_image

Solver

определяет паттерн / нарратив

выбирает файл картинки

прикладывает текстовое описание

Handler

отображает картинку (send_tracked_photo)

не анализирует содержимое

GPT

получает description как факт

использует его в объяснении

не задаёт вопросов про рисунок

🧠 Ключевая архитектурная формула

Картинка — это UI.
Описание — это математика.
Solver — источник истины.

📎 Статус

Контракт зафиксирован

Изменения допускаются только осознанно и централизованно

Все будущие задания с картинками обязаны ему следовать


## Контракт help_image для модуля «Помощь»
1. Назначение

Контракт help_image используется для согласованного отображения учебной картинки и её словесного описания в модуле «Помощь» бота «Матюня».

Ключевая идея:

❌ GPT не видит изображение;

✅ GPT получает точное текстовое описание именно той картинки, которая показана ученику;

❌ GPT не додумывает расположение точек, дуг и углов;

✅ Вся информация берётся из solution_core.

Это необходимо для:

сохранения доверия к боту;

предотвращения фантазирования GPT;

корректных ответов на вопросы вида: «почему эта дуга?», «почему угол ABC?».

2. Где используется

Контракт применяется:

в solution_core, возвращаемом solver’ом;

в help-handler’е (например, help_handler_16.py);

при формировании GPT-диалога (dialog_context).

common.py не знает про картинки и не работает с help_image.

3. Место в solution_core

Объект help_image является опциональным, но если он присутствует, обязан соответствовать контракту.

{
  "help_image": { ... }
}
4. Структура help_image
"help_image": {
  "file": "string",
  "schema": "string",
  "params": { }
}
4.1. file (обязательное)

Имя файла изображения, которое будет показано ученику.

без пути;

путь формируется в handler’е:

matunya_bot_final/non_generators/task_<N>/assets/<file>

Пример:

"file": "help_cyclic_quad_angles_abcd_diags_arc_bc.png"
4.2. schema (обязательное)

Код схемы изображения. Используется для выбора шаблона текстового описания.

Примеры:

cyclic_quad_opposite_angles

cyclic_quad_part_sum

4.3. params (обязательное)

Параметры конкретной картинки.

Содержит только факты, никаких текстов.

Типичные поля для задания 16:

"params": {
  "vertices": ["A", "B", "C", "D"],
  "arc_name": "BC",
  "angle_given": "∠ADB",
  "angle_given_value": 47,
  "angle_equal": "∠BAC",
  "angle_target": "∠ADC"
}

Важно:

параметры зависят от конкретного файла;

8 разных картинок → 8 разных наборов params;

GPT никогда не угадывает обозначения.

5. Ответственность компонентов
Solver (обязан):

выбрать корректный file;

передать точные params;

не формировать текст.

Help-handler (обязан):

вывести картинку через send_tracked_photo;

передать help_image дальше в dialog_context.

GPT:

использует только описание, полученное из help_image;

не задаёт вопросов вида «покажи рисунок».

6. Пример (Task 16)
"help_image": {
  "file": "help_cyclic_quad_angles_abcd_diags_arc_bc.png",
  "schema": "cyclic_quad_opposite_angles",
  "params": {
    "vertices": ["A", "B", "C", "D"],
    "arc_name": "BC",
    "angle_given": "∠ADB",
    "angle_given_value": 47,
    "angle_equal": "∠BAC",
    "angle_target": "∠ADC"
  }
}
7. Ключевые запреты

❌ Не хранить описание картинки в виде готового текста ❌ Не генерировать описание внутри GPT ❌ Не смешивать логику картинок с common.py

8. Итог

Контракт help_image — это мост между:

визуальным обучением (картинка);

строгой математикой (solution_core);

диалогом с учеником (GPT).

Без него GPT неизбежно начинает фантазировать.
