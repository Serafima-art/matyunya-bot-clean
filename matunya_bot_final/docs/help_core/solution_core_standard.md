Документ №1: "ГОСТ-2025: Стандарт solution_core"

# ГОСТ-2026: Стандарт Структуры solution_core v2.2: 

## 1. Назначение

`solution_core` — это стандартизированный JSON-совместимый словарь (dict), который является универсальным форматом для передачи структурированного решения от любого "Решателя" (`solver`) к "Оживителю" (`humanizer`) и другим компонентам системы.

## 2. Структура v2.1

```python
{
    # --- БЛОК 1: ИДЕНТИФИКАЦИЯ И МЕТАДАННЫЕ ---
    "question_id": str,
    "question_group": str,

    # --- БЛОК 2: ПЕДАГОГИЧЕСКАЯ ЧАСТЬ ---
    "explanation_idea": str,
    
    # --- БЛОК 3: МАТЕМАТИЧЕСКОЕ ЯДРО ---
    "calculation_steps": [
        {
            "step_number": int,
            "description": str,
            "formula_representation": Optional[str],
            "calculation_result": str,
            "result_unit": Optional[str],
            "visual_instruction": Optional[{
                "source_image": str,
                "tool": str, # e.g., "highlighter", "add_arrow"
                "params": dict
            }]
        },
    ],

    # --- БЛОК 4: ИТОГОВЫЙ РЕЗУЛЬТАТ ---
    "final_answer": {
        "value_machine": Union[float, int, str],
        "value_display": str,
        "unit": Optional[str],
        "visual_instruction": Optional[{...}] # Та же структура
    },
    
    # --- БЛОК 5: ВСПОМОГАТЕЛЬНАЯ ИНФОРМАЦИЯ ---
    "validation_code": Optional[str],
    "hints": List[str]
}
3. Описание Полей
question_id (str): Уникальный ID подтипа. (e.g., "tires_q1_min_width", "11_match_signs_a_c"). Обязательно.
question_group (str): Общая группа заданий. (e.g., "Q1_TABLE", "SIGNS_A_C"). Обязательно.
explanation_idea (str): Ключевая идея решения в 1-2 предложениях. Обязательно.
calculation_steps (List[dict]): Список шагов решения. Обязательно.
step_number (int): Номер шага.
description (str): Текстовое описание действия.
formula_representation (Optional[str]): Текстовое представление формулы (e.g., "D = 2H + d * 25.4").
calculation_result (str): Результат вычисления с подставленными числами (e.g., "2*55 + 17*25.4 = 541.8 мм").
result_unit (Optional[str]): Единица измерения результата этого шага.
visual_instruction (Optional[dict]): Инструкция для "Визуализатора".
source_image (str): Путь к исходному изображению.
tool (str): Название инструмента (e.g., "highlighter", "add_arrow").
params (dict): Параметры для инструмента.
final_answer (dict): Итоговый результат. Обязательно.
value_machine (Union[float, int, str]): Ответ в машинном формате для валидации.
value_display (str): Ответ в формате для показа ученику.
unit (Optional[str]): Финальная единица измерения.
visual_instruction (Optional[dict]): Финальная визуализация.
validation_code (Optional[str]): Python-код для автоматической проверки.
hints (List[str]): Список полезных советов. Обязательно.



. "ГОСТ-2026: Стандарт solution_core" (Неизменен)
Этот стандарт остается незыблемым фундаментом всей системы. Любой "Решатель" (solver) обязан возвращать словарь, соответствующий этой структуре. Это "универсальный язык", который понимают оба наших "конвейера" и система "Передачи Эстафеты".
Краткая структура:
code
Python
{
    "question_id": str,
    "question_group": str,
    "explanation_idea": str,
    "calculation_steps": [ { ... } ],
    "final_answer": { ... },
    "hints": List[str],
    "validation_code": Optional[str],     // Зарезервировано для будущего
    "visual_instruction": Optional[dict]  // Зарезервировано для будущего
}

Раздел 3: Математическое Ядро (calculation_steps)
calculation_steps — это список словарей, где каждый словарь описывает один логический шаг решения.
Структура одного шага:
code
Python
{
    "step_number": int,
    "description": str,
    
    # --- БЛОК ФОРМУЛ (одно из полей обязательно, если шаг содержит математику) ---
    "formula_representation": Optional[str], # Для простых формул и выражений
    "formula_general": Optional[str],      # ★★★ НОВОЕ ★★★ Для общих формул (D = b² - 4ac)
    "formula_calculation": Optional[str],  # ★★★ НОВОЕ ★★★ Для формул с подставленными числами
    
    "calculation_result": Optional[str],
    "result_unit": Optional[str]
    # "visual_instruction" остается для будущего
}
Описание Новых Полей:
formula_general (Optional[str]): Общая математическая формула, которая применяется на данном шаге. Пример: "D = b² - 4ac".
formula_calculation (Optional[str]): Та же формула, но с подставленными в нее конкретными числами из задачи и, возможно, с результатом. Пример: "D = (5)² - 4·2·(-3) = 49".
Как их использовать:
"Решатель" должен использовать эти поля для детализации вычислений, как мы и договорились в "Золотом Эталоне". "Очеловечиватель" будет ожидать эти ключи и форматировать их соответствующим образом.


4. Пользовательский Сценарий (Унифицирован)
Ученик нажимает 🆘 Помощь.
Система (по одному из двух "конвейеров") генерирует и отправляет Панель Помощи, помещая ее в категорию help_panels.
Под панелью находится централизованная клавиатура с кнопками ❌ Скрыть помощь и ❓ Задать вопрос.
При нажатии на ❓ Задать вопрос запускается универсальный диалоговый контроллер (gpt_dialog_control_handler.py), который переводит пользователя в состояние GPState.in_dialog и управляет всей дальнейшей перепиской.
Вся переписка с GPT помечается категорией dialog_messages и удаляется по кнопке ✅ Всё понятно, спасибо!, не затрагивая основную Панель Помощи.
5. Планы на Будущее
"Операция 'Спойлер'": Внедрить использование тега <tg-spoiler> для финальных ответов в обоих "конвейерах", чтобы усилить педагогический эффект, как и было задумано в первоначальной философии.


Новая Структура solution_core для Двух Решений:

Для паттерна powers_with_fractions задание 6 (и только для него, при наличии общего множителя) solution_core должен иметь следующую расширенную структуру:
code
Python
{
    "question_id": "...",
    "explanation_idea": "Эту задачу можно решить двумя способами...",
    "calculation_paths": [  # Вместо "calculation_steps"
        {
            "path_title": "Способ 1: Стандартный (по порядку действий)",
            "steps": [ ...список шагов для первого способа... ]
        },
        {
            "path_title": "Способ 2: Рациональный (вынесение множителя)",
            "is_recommended": True, # Флаг для выделения лучшего способа
            "steps": [ ...список шагов для второго способа... ]
        }
    ],
    "final_answer": { ... },
    "hints_keys": [...]
}





Ответы на Вопросы по powers_with_fractions:
1. Условие для двух способов решения:
Твое понимание близко, но не совсем точное. Давай определим условие проще и надежнее.
Два способа решения нужны, если в expression_tree общий множитель встречается дважды.
Пример 1: 21 · (2/7)² + 2/7
expression_tree будет содержать узел {"type": "common", "value": [2, 7]} ДВАЖДЫ: один раз как самостоятельный операнд, и второй раз — как основание степени. Это условие выполняется -> Генерируем два решения.
Пример 2: 55 · (2/11)² + 12 · 2/11
Здесь 2/11 тоже встречается дважды. Условие выполняется -> Генерируем два решения.
Пример 3: 40 · (1/2)³ + 3
Здесь 1/2 встречается только один раз (в степени). Число 3 — это другой операнд. Условие НЕ выполняется -> Генерируем только ОДНО (стандартное) решение.
Твоя проверка "целое число делится на знаменатель" — это хорошее наблюдение, но оно может быть сложным в реализации. Проверка на дублирование узлов в дереве — гораздо проще и надежнее.
2. Структура для одного способа:
Абсолютно верно. Если генерируется только один способ решения (стандартный), используй обычную структуру с ключом calculation_steps. Новая структура с calculation_paths нужна только для случаев с двумя решениями.
3. Паттерн powers_of_ten:
Абсолютно верно. Для этого паттерна всегда один стандартный способ решения (через группировку коэффициентов и сложение показателей степеней).
Ответы на Вопросы по Формату:
4. Поля формул:
Используй formula_general + formula_calculation. Это наш новый, более детализированный стандарт.
formula_general — для теоретических формул (например, aⁿ · aᵐ = aⁿ⁺ᵐ).
formula_calculation — для вычислений с конкретными числами из примера (например, 10³ · 10⁻¹ = 10³⁻¹ = 10²).
formula_representation можно использовать, чтобы показать текущее состояние всего выражения на данном шаге.
5. Формат дробей в формулах:
Используй самый простой и надежный формат, который не требует специальных символов Unicode.
(1/2)³ — Да, идеально. Скобки четко показывают, что возводится в степень.
1³/2³ — Да, идеально. Показывает раскрытие степени.
Главное — не использовать специальные символы вроде ¹⁄₂, так как они могут некорректно отображаться на разных устройствах. Простота и читаемость — наш приоритет.
6. Hints (Подсказки):
Ожидаются специфичные для каждого паттерна подсказки.
Для powers_with_fractions нужны подсказки про порядок действий и про возможность вынесения общего множителя.
Для powers_of_ten — про свойства степеней ((a·b)ⁿ = aⁿ·bⁿ и aⁿ·aᵐ = aⁿ⁺ᵐ).
Твоя задача — сгенерировать соответствующие hints_keys в solution_core, а мы потом добавим для них текст в "Оживитель".
Финальное Заключение для Claude:
"Твои уточнения были критически важны. Теперь у нас полное совпадение видения. Мы утверждаем все предложенные тобой решения с учетом наших комментариев.
Даю команду: приступай к написанию кода!"
