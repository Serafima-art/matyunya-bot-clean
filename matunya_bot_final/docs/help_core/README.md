Документ №2 v2.0: "Архитектура Модуля Помощи (help_core)"

# Архитектура Модуля Помощи (`help_core`) - v3.0

## 1. Философия: "Интерактивное Решение"

Мы отказались от сложной, многоступенчатой системы помощи в пользу **единого, мощного и педагогически выверенного** инструмента. Наша цель — не провести ученика по "лестнице", а предоставить ему **полный разбор** решения, дав при этом **возможность подумать самостоятельно**.

**Ключевые принципы:**
*   **Одно Окно:** Вся помощь выдается в одном, красиво отформатированном сообщении.
*   **Интерактивный Спойлер:** Ключевые вычисления и финальный ответ "спрятаны" под спойлер, поощряя ученика к самостоятельному расчету.
*   **Ответственность Ученика:** Мы предоставляем лучший инструмент для разбора, но оставляем за учеником выбор — пользоваться им пассивно или активно.

## 2. Разделение Систем Поддержки

В "Матюне" существует два независимых канала поддержки:

*   **`📚 Теория`:** Краткий, концентрированный **справочник** по теме, вызываемый отдельной кнопкой. (Реализуется отдельно, с помощью `theory_dispatcher`).
*   **`🆘 Помощь`:** Полный, пошаговый **разбор** конкретного, сгенерированного задания.

Эти системы не дублируют, а дополняют друг друга.

# 🧭 Архитектура "Консилиума" — Система Помощи Матюни

Обновлённая версия механизма помощи работает по принципу  
**«Диспетчер → Решатель → Оживитель»**  
и полностью масштабируется на все 25 заданий.

---

## 🔹 1. `dispatchers/help_handler.py` — Единый Диспетчер

**Роль:** самый “глупый” слой.  
Ничего не решает и не форматирует — просто принимает нажатие `[🆘 Помощь]`  
и передаёт управление нужному хендлеру по номеру задания.

### Алгоритм:

1. Ловит callback `action="request_help"`;
2. Определяет номер задания (`task_type`);
3. Обращается к таблице маршрутизации:

```python
HELP_ROUTERS = {
    11: handle_task_11_help,
    20: handle_task_20_help,
    # 6, 7, 8 и др. — будут подключены позже
}
Передаёт управление в нужный help-хендлер;

Если хендлер не найден — выводит стандартное сообщение
«Решение пока недоступно 😔».

💡 “Диспетчер” не знает ничего о содержимом решений,
он лишь маршрутизирует запрос по номеру задания.

🔹 2. dispatchers/task_11/help_handler_11.py и dispatchers/task_20/help_handler_20.py
Роль: “умные” help-хендлеры,
каждый из которых знает особенности своего задания.

Типовой алгоритм (handle_task_X_help):
Отправляет сообщение: «🔄 Генерирую решение…»

Извлекает данные задания из FSM (state.get_data());

Вызывает решатель через call_dynamic_solver();

Получает solution_core и передаёт в гуманизатор (humanize_solution_X());

Создаёт клавиатуру окна помощи (create_solution_keyboard());

Отправляет готовое решение пользователю (send_tracked_message()).

Все help-хендлеры построены по единому шаблону,
чтобы их можно было легко копировать для новых заданий.

🔹 3. help_core/solvers/ — Решатели
Каждый подтип имеет собственный solve():

Формирует структуру solution_core
(идея, шаги, ответ, подсказки);

Не содержит HTML и эмодзи;

Возвращает результат в стандартизированном виде.

Пример:
help_core/solvers/task_20/rational_inequalities_solver.py

🔹 4. help_core/humanizers/ — Оживители
Оживляют “сухой” solution_core:
добавляют HTML-разметку, эмодзи, блоки и форматирование.

solution_humanizer.py — общий шаблон;

template_humanizers/task_11_humanizer.py и task_20_humanizer.py — специализированные версии.

Типовые блоки:

💡 Идея решения

🔎 Шаги

✅ Ответ

💭 Полезно помнить

🔹 5. keyboards/inline_keyboards/help_core_keyboard.py
Создаёт стандартную клавиатуру окна помощи:

Копировать код
❌ Закрыть помощь | ❓ Задать вопрос
Функция:

python
Копировать код
def create_solution_keyboard(task_subtype: str, task_type: int) -> InlineKeyboardMarkup:
Используется всеми help-хендлерами,
обеспечивая единообразие интерфейса.

🪄 Бонус-идея (заложена на будущее)
В help_handler.py уже добавлен задел для масштабирования:

python
Копировать код
# HELP_ROUTERS = {
#     6: handle_task_6_help,
#     7: handle_task_7_help,
#     8: handle_task_8_help,
#     11: handle_task_11_help,
#     20: handle_task_20_help,
# }
Чтобы подключить новое задание, достаточно добавить строку:

python
Копировать код
HELP_ROUTERS[15] = handle_task_15_help
и всё заработает автоматически —
без правок в остальном коде.

🧩 Итоговая структура проекта
bash
Копировать код
help_core/
│
├── dispatchers/
│   ├── help_handler.py                 # единый диспетчер
│   ├── task_11/help_handler_11.py      # умный хендлер для №11
│   └── task_20/help_handler_20.py      # умный хендлер для №20
│
├── solvers/
│   └── task_20/rational_inequalities_solver.py
│
├── humanizers/
│   ├── solution_humanizer.py
│   └── template_humanizers/
│       ├── task_11_humanizer.py
│       └── task_20_humanizer.py
│
└── keyboards/
    └── inline_keyboards/help_core_keyboard.py
🪶 Документ обновлён: октябрь 2025
Автор: Олюшка и Матюня 🤖

*   **`solvers/`:** "Мозговой Центр". Содержит "Решателей" для каждого подтипа.
    *   **Обязанность:** Каждый "Решатель" **обязан** возвращать `solution_core` в строгом соответствии с **"ГОСТ-2025: Стандарт solution_core"**.

*   **`humanizers/solution_humanizer.py`:** "Голос Матюни" ("Оживитель").
    *   **Задача:** Принимает `solution_core` и, используя GPT и системный промпт, превращает его в "живой", отформатированный текст.
    *   **Ключевая Особенность:** Промпт `HUMANIZER_SYSTEM_PROMPT` содержит **строгое указание** оборачивать финальный ответ и ключевые расчеты в HTML-тег `<tg-spoiler>`.

## 4. Пользовательский Сценарий

1.  Ученик нажимает кнопку **`🆘 Помощь`** под заданием.
2.  Бот присылает **одно** сообщение, содержащее:
    *   Дружелюбное вступление.
    *   Полный, пошаговый алгоритм решения.
    *   Предупреждение "Попробуй решить сам!".
    *   Финальные расчеты и ответ, **скрытые под спойлером**.
3.  Под этим сообщением находятся кнопки `[❓ Задать вопрос]` и `[❌ Скрыть помощь]`.


Архитектурная Спецификация v4.0: Гибридная Система Помощи (help_core)
Дата: 05.10.2025
Статус: Утверждено
1. Философия: Педагогика и Гибкость
Мы эволюционировали от простой выдачи решений к созданию гибкой обучающей экосистемы. Наша философия строится на трех китах:
Дать Ученику Выбор: Мы предоставляем мощные инструменты для разбора, но не навязываем их. Ученик сам решает, как глубоко погружаться в решение.
Контролируемый Креатив: Мы используем мощь GPT для создания "живых", человечных объяснений, но направляем его креативность с помощью наших "Золотых Наборов", обеспечивая высокое педагогическое качество.
Правильный Инструмент для Задачи: Мы признаем, что не все задачи требуют одинакового подхода. Наша система теперь гибридная — она может использовать как быстрые, шаблонные объяснения для простых алгоритмических задач, так и глубокую GPT-обработку для сложных, "сюжетных" заданий.
2. Архитектура: "Консилиум Экспертов" v4.0
Общий принцип "Диспетчер -> Решатель -> Оживитель" остается в силе, но компонент "Оживитель" был разделен на два независимых "конвейера".
2.1. Два Конвейера Системы Помощи
Аспект	Конвейер "Полный Цикл GPT"	Конвейер "Гибридный"
Применение	Задания 1-5 ("Шины", "Печи" и т.д.)	Задание 11 (Графики) и другие алгоритмические задачи
Генерация Помощи	Решатель -> solution_core -> Оживитель-GPT -> HTML-текст	Решатель -> solution_core -> Оживитель-Шаблон -> HTML-текст
Генерация Диалога	Тот же "прогретый" GPT продолжает диалог	"Холодный" GPT "прогревается" solution_core и "Внешним Мозгом"
Плюсы	Максимальная "человечность", идеален для "сюжетов"	Мгновенная выдача Помощи, дешевле, 100% предсказуемый результат
Минусы	Дольше (сетевой запрос), дороже (API)	Менее "творческий" текст в основной панели Помощи
2.2. "Внешний Мозг": Система "Золотых Наборов"
Это ключевая инновация нашей гибридной модели.
Источник Знаний: Все наши лучшие педагогические наработки (метафоры, аналогии, алгоритмы) хранятся вне кода, в централизованной Google Таблице.
"Библиотекарь" (golden_set_reader.py): Специальный модуль с кэшированием, который по запросу "ходит" в Google Таблицу и извлекает оттуда релевантный "Золотой Набор" для конкретного подтипа задания.
Интеграция: Полученный "Набор" передается в системный промпт GPT в качестве "палитры стиля", направляя его креативность и обеспечивая высокое качество и узнаваемый стиль "Матюни".


🧩 3. Старшие официанты (Help Dispatchers per Task)

Каждый тип задания имеет собственного “старшего официанта” — отдельный help_handler_X.py, который знает всё о своих “поварах” (решателях) и “декораторах” (оживителях).

Задание	Файл	Роль	Особенности
1–5 (Группа)	task_1_5/handle_group_1_5_help.py	🧭 “Супер-официант” группы 1–5	Отвечает за все задания из блока 1–5 (например, «Шины», «Форматы бумаги», «Теплицы» и др.). Сам выбирает нужного решателя по subtype и question_num.
 ↳ Шины	task_1_5/help_handler_1_5.py	📋 Меню “поваров”	Содержит SOLVER_DISPATCHER, который сопоставляет номера вопросов (q1…q6) с функциями tires_qN_solver.solve(). Используется внутри handle_group_1_5_help.
11	task_11/help_handler_11.py	🎯 Официант столика №11	Работает с подтипами match_signs_a_c, match_signs_k_b, и т.д. Использует шаблонный оживитель task_11_humanizer.py.
20	task_20/help_handler_20.py	🧮 Официант столика №20	Обрабатывает сложные подтипы (polynomial_factorization, radical_equations, rational_inequalities) и передаёт их оживителю task_20_humanizer.py.
📘 Принцип работы

Пользователь нажимает кнопку 🆘 Помощь.

Главный диспетчер (help_handler.py) определяет task_number и вызывает нужного “официанта” через HelpRegistry.

“Официант” (например, handle_group_1_5_help)
— получает данные из FSM,
— находит соответствующего “повара” (solve()-функцию) через SOLVER_DISPATCHER,
— получает solution_core,
— передаёт его “декоратору” (GPT-оживителю или шаблонному humanizer-модулю).

Готовый, дружелюбный текст решения отправляется пользователю,
вместе с клавиатурой действий (Закрыть / Задать вопрос).

🪄 Почему для заданий 1–5 используется один хендлер

Блок 1–5 объединяет несколько родственных заданий, где структура логики одинакова — это табличные и текстовые задания.
Поэтому один общий диспетчер (handle_group_1_5_help) обслуживает всю группу,
а внутри уже выбирает конкретное “меню” — например, help_handler_1_5.py для «Шин».

Это делает систему гибкой:

не нужно 5 отдельных диспетчеров;

новые подтипы (например, “Форматы бумаги”) можно добавить, просто добавив новый help_handler_X.py и SOLVER_DISPATCHER;

общий стиль и интерфейс сохраняются.
