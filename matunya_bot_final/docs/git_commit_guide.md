🧭 Шпаргалка «Коммиты Матюни»
🧩 1. Добавляешь новую функцию, подтип, модуль

💡 Используй feat:

git commit -m "feat: добавлен подтип задания 20 polynomial_factorization"
git commit -m "feat: реализован humanizer для help_system"
git commit -m "feat: добавлен блок 'Полезно помнить' в решения"

🛠 2. Исправляешь ошибки, баги, неправильную логику

💡 Используй fix:

git commit -m "fix: исправлен вывод теории в task_11_handler"
git commit -m "fix: устранена ошибка спойлеров в помощи"
git commit -m "fix: корректно отображается клавиатура после решения"

📘 3. Обновляешь документацию, памятки, Git-инструкции

💡 Используй docs:

git commit -m "docs: добавлена памятка по веткам"
git commit -m "docs: обновлён раздел 'Работа в нескольких папках'"
git commit -m "docs: уточнён Git.md для Кодекса"

✨ 4. Улучшаешь стиль или читаемость кода (но не саму логику)

💡 Используй style:

git commit -m "style: отформатирован help_handler по PEP8"
git commit -m "style: очищен .gitignore"
git commit -m "style: упорядочены импорты в gpt_utils.py"

♻️ 5. Переписываешь кусок логики, но смысл остаётся тем же

💡 Используй refactor:

git commit -m "refactor: объединён общий код генераторов task_7"
git commit -m "refactor: улучшена структура inline_callback.py"
git commit -m "refactor: вынесен phrase_loader в отдельный модуль"

⚙️ 6. Технические изменения, обновление зависимостей, .env, .gitignore

💡 Используй chore:

git commit -m "chore: обновлён .gitignore и requirements.txt"
git commit -m "chore: добавлен exclude для .env"
git commit -m "chore: удалены временные файлы и __pycache__"

💬 Формула-золотник

🔹 [тип]: [что именно изменено]
🔹 Всё по-русски, коротко, без точки на конце
🔹 Один коммит = одно логическое изменение

# 🧭 Шпаргалка «Коммиты Матюни»

> 🌿 Один проект — один стиль коммитов.  
> Каждый коммит должен чётко отвечать на вопрос: **что изменилось и зачем.**

---

## 💡 Формат
<тип>: <краткое описание изменения>

yaml
Копировать код

📌 Всё по-русски, без точки в конце.  
Один коммит = одно логическое изменение.

---

## 🔹 Основные типы коммитов

| Тип | Когда использовать | Пример |
|-----|--------------------|--------|
| `feat:` | Новая функция, подтип, блок или раздел | `feat: добавлен подтип задания 20 polynomial_factorization` |
| `fix:` | Исправление ошибок, багов, некорректной логики | `fix: исправлен вывод теории в task_11_handler` |
| `docs:` | Обновления документации, памяток, Git-инструкций | `docs: добавлена памятка по веткам` |
| `style:` | Изменения в стиле кода, отступах, комментариях | `style: очищен .gitignore` |
| `refactor:` | Переписан код без изменения логики | `refactor: объединён общий код генераторов task_7` |
| `chore:` | Технические обновления, зависимости, файлы среды | `chore: обновлён .gitignore и requirements.txt` |

---

## 🧩 Примеры реальных коммитов Матюни

### ✨ Новые возможности
```bash
feat: добавлен humanizer для help_system
feat: реализован блок 'Полезно помнить' в решениях
🛠 Исправления
bash
Копировать код
fix: устранена ошибка спойлеров в помощи
fix: корректно отображается клавиатура после решения
📘 Документация
bash
Копировать код
docs: добавлен раздел 'Работа в нескольких папках'
docs: обновлён Git.md для Кодекса
🧹 Стиль
bash
Копировать код
style: отформатирован help_handler по PEP8
style: упорядочены импорты в gpt_utils.py
♻️ Рефакторинг
bash
Копировать код
refactor: вынесен phrase_loader в отдельный модуль
refactor: улучшена структура inline_callback.py
⚙️ Служебные задачи
bash
Копировать код
chore: добавлен exclude для .env
chore: удалены временные файлы и __pycache__
✅ Быстрая формула
🔹 feat: — новая функция
🔹 fix: — ошибка
🔹 docs: — документация
🔹 style: — внешний вид
🔹 refactor: — улучшение кода
🔹 chore: — обслуживание

🎉 Напоминание Капитана
Если Git пишет nothing to commit, working tree clean —
всё сохранено, проект стабилен, можно спать спокойно 😴


🧭 Путь изменения от VS Code до GitHub
🖋️ 1. Пишешь код в VS Code
      ↓
💾 2. Автосохранение (VS Code сохраняет файл локально)
      ↓
🌿 3. git add .
      — говоришь Git: "Вот, запомни эти изменения."
      ↓
📦 4. git commit -m "feat: добавлен humanizer для help_system"
      — фиксируешь их в истории проекта
      ↓
☁️ 5. git push origin feat/help-system-dev
      — отправляешь сохранённую версию на GitHub
      ↓
✨ Результат: изменения видны в репозитории на GitHub


🔧 Работаешь в ветке feat/help-system-dev

(все команды из активного окружения .venv)

git add .
git commit -m "docs: обновлён раздел о коммитах"
git push origin feat/help-system-dev


✅ — всё правильно:

add — подготовила изменения,

commit — зафиксировала,

push — отправила в облако.

🔍 Проверка состояния
git status


— всё верно, покажет, чисто ли дерево и есть ли несохранённые изменения.

🌿 Слияние в стабильную ветку main

(когда закончила эксперимент и всё протестировала):

git checkout main        # переключаешься на стабильную ветку
git pull origin main      # обновляешь main с GitHub на всякий случай
git merge feat/help-system-dev   # вливаешь изменения из ветки разработки
git push origin main      # отправляешь обновлённый main в облако


💬 теперь main и feat/help-system-dev синхронизированы.
После этого можно снова работать в feat/help-system-dev, если начнёшь новую задачу.

🪄 Короткий конспект:

Рабочая ветка — творим ✨
Коммит — фиксируем 💾
Push — отправляем ☁️
Merge в main — стабилизируем 🚀
