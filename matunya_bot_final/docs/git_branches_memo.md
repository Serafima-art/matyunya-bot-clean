# 🧭 Git-Памятка Матюни

## 🌿 Ветки
- **main** — стабильная ветка, чистовая версия бота.  
- **feat/help-system-dev** — рабочая ветка для новых фич, тестов и правок.

---

## 💾 Правила работы

1️⃣ **Работа ведётся в ветке `feat/help-system-dev`**  
   Все задачи от Кодекса и изменения делаются только здесь.

2️⃣ **Перед переключением в `main` — обязательно коммит!**
   ```bash
   git add .
   git commit -m "фикс: что сделано"

   ## 🧰 Если работаешь в нескольких папках (Матюня и Мастерская)

В проекте Матюни есть две главные рабочие папки:
- `matunya_bot_final` — код и логика бота  
- `МАСТЕРСКАЯ` — материалы, карточки, CSV, docs, теоретические заметки

🧩 Обе находятся внутри корневой папки `matunya`, где лежит `.git`.

Это значит:
- Git **видит обе папки как одну систему** — весь проект под единым контролем.  
- Все изменения (и код, и материалы) сохраняются одним коммитом.

---

### ✅ Как работать правильно:

1️⃣ Вноси изменения где угодно — в `matunya_bot_final` или `МАСТЕРСКАЯ`.  
2️⃣ Всегда выполняй команды Git **только из корня проекта**:
   ```bash
   (.venv) C:\Users\user\Documents\matunya>
   git add .
   git commit -m "fix: обновлены файлы Матюни и Мастерской"
   git push origin feat/help-system-dev
3️⃣ Git зафиксирует все изменения сразу — во всех подпапках.
4️⃣ Не создавай отдельный .git в МАСТЕРСКАЯ, чтобы не нарушить общую структуру.

📌 Главное правило
Один проект — один .git в корне.
Работай в любой папке, но командуй всегда из материнской директории:


Копировать код
(.venv) C:\Users\user\Documents\matunya>



🔄 Цикл жизни файла в Git
🟩 1. Изменила файл в VS Code
     ↓
     (Git видит изменения)
     команда: git status
     → покажет “modified: matunya_bot_final/docs/Git.md”

🟦 2. Подготовила к сохранению
     ↓
     команда: git add .
     → файл переходит в staged (готов к коммиту)

⚪ 3. Зафиксировала изменения
     ↓
     команда: git commit -m "docs: обновлён раздел о коммитах"
     → файл побелел, Git сохранил версию в истории

☁️ 4. Отправила на GitHub
     ↓
     команда: git push origin feat/help-system-dev
     → теперь изменения в облаке 🌍

💬 И запомни формулу Матюни:

VS Code сохраняет 👉 Git фиксирует 👉 GitHub хранит


📘 Шпаргалка «6 команд Git Матюни»
💻 Команда	🌿 Что делает	💬 Простыми словами
git status	Показывает текущее состояние проекта	🔍 Проверка: есть ли несохранённые файлы, на какой ветке ты сейчас
git add .	Добавляет все изменённые файлы в список для коммита	📦 Сказать Git: “Вот эти файлы нужно запомнить”
git commit -m "..."	Фиксирует изменения в истории проекта	💾 Сохранить момент — как «сделать снимок» состояния кода
git push origin feat/help-system-dev	Отправляет все зафиксированные изменения на GitHub	☁️ Отправить всё в облако, чтобы не потерять
git checkout main	Переключиться на другую ветку (в данном случае — стабильную)	🔄 Переход в “чистую” ветку, где только проверенный код
git merge feat/help-system-dev	Сливает изменения из ветки разработки в основную	🤝 Объединить результаты работы с основной версией

🧭 Полезная формула Матюни
Изменил → add → commit → push → merge → main


💬 или по-человечески:

✏️ Сделал → 💾 Сохранил → ☁️ Отправил → 🚀 Объединил


🌿 Когда сливать feat/help-system-dev → main
🟢 Вариант 1. После каждого “завершённого этапа”

💬 Самый надёжный и чистый способ.

Когда ты закончила логический кусок работы — например:

реализовала новый подтип,

обновила документацию,

поправила вывод “Помощи” —

тогда:

git add .
git commit -m "feat: добавлен новый подтип задания 20"
git push origin feat/help-system-dev
git checkout main
git merge feat/help-system-dev
git push origin main


✅ main теперь стабильная, актуальная,
✅ feat/help-system-dev можно использовать дальше.

🟡 Вариант 2. Можно копить коммиты несколько дней подряд

Если ты делаешь серию правок (например, большую фичу, тесты и хендлеры),
то можно коммитить и пушить каждый день,
а вливать в main — только когда закончишь весь этап.

💡 Главное — коммитить и пушить регулярно,
чтобы не потерять ничего, даже если VS Code или Render внезапно упадёт.

🔴 Вариант, которого лучше избегать

Работать неделями в feat/help-system-dev,
не вливать в main и не пушить.

Тогда ветка может “разъехаться” с main, и потом merge будет сложнее.

💬 Простое правило Матюни:

🗓️ Коммить — каждый день,
☁️ Пушить — после каждой сессии работы,
🌿 Мержить в main — когда закончила логическую задачу.

## 🌿 Когда мержить ветку разработки (`feat/help-system-dev`) в `main`

> 💬 Ветка `feat/help-system-dev` — это твоя лаборатория.  
> Ветка `main` — это музей: там только готовые, стабильные экспонаты 🖼️

---

### 🟢 Когда мержить

✅ **После завершённого этапа**  
Например:
- добавила новый подтип;
- исправила форматирование решения;
- обновила документацию.

```bash
git add .
git commit -m "feat: добавлен подтип задания 20" --no-verify — пробел перед --no-verify важен.
git push origin feat/help-system-dev
git checkout main
git merge feat/help-system-dev
git push origin main
Теперь main — актуальная, стабильная версия.
Можно спокойно продолжать эксперименты в feat/help-system-dev.

🟡 Можно копить коммиты
Если делаешь серию правок в одной теме,
можно несколько дней подряд коммитить и пушить только в feat/help-system-dev.

💡 Главное — пушить каждый день, чтобы ничего не потерялось.

🔴 Когда нельзя затягивать
Не оставляй ветку feat/help-system-dev без merge неделями —
ветки могут “разъехаться”, и потом будет трудно соединить.

📅 Простое правило Матюни
Действие	Частота
💾 git commit	Каждый день, как сохраняешь прогресс
☁️ git push	После каждой сессии работы
🤝 git merge	Когда закончила логический кусок


конкретный файл:

git add matunya_bot_final/docs/Git.md


или всё сразу:

git add .


---

## 🧩 Когда стоит добавить pre-commit и линтеры

> 💬 Пока Матюня растёт, pre-commit можно не трогать.  
> Но когда бот станет большим и в проекте появится команда —  
> пора ввести “редактора кода”, который всё проверяет автоматически.

---

### 🌿 Когда пора подключать pre-commit

| Признак | Комментарий |
|----------|--------------|
| Проект стал объёмным (>10 000 строк кода) | Много файлов, легко что-то забыть |
| Появилось несколько разработчиков | Нужно, чтобы все писали код в одном стиле |
| В коммитах часто правятся мелочи | Значит, нужна авто-проверка перед коммитом |
| Начался релиз на Render | Нужно держать код идеально чистым |

---

### 🧰 Что установить (на будущее)

| Инструмент | Что делает | Команда установки |
|-------------|-------------|------------------|
| **flake8** | Проверяет стиль и ошибки | `pip install flake8` |
| **black** | Автоматически форматирует код | `pip install black` |
| **isort** | Сортирует импорты | `pip install isort` |
| **mypy** | Проверяет типы данных | `pip install mypy` |

---

### ⚙️ Как это работает

1️⃣ Устанавливаешь pre-commit:
```bash
pip install pre-commit
2️⃣ Создаёшь файл .pre-commit-config.yaml:

yaml
Копировать код
repos:
  - repo: https://github.com/psf/black
    rev: 23.7.0
    hooks:
      - id: black
  - repo: https://github.com/PyCQA/flake8
    rev: 6.1.0
    hooks:
      - id: flake8
  - repo: https://github.com/PyCQA/isort
    rev: 5.12.0
    hooks:
      - id: isort
3️⃣ Активируешь:

bash
Копировать код
pre-commit install
Теперь при каждом git commit код автоматически проверяется 🧹

💬 Напоминание Матюни
Не торопись подключать линтеры —
сначала сделай код живым и добрым,
потом уже идеально ровным 💛



