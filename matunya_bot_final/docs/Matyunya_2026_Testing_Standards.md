# 🧪 ГОСТ-ТЕСТЫ "МАТЮНЯ-2026"

## 1. Назначение
Документ определяет единые стандарты для тестов генераторов и валидаторов заданий проекта **«Матюня»** (ОГЭ-2026).

## 2. Ключевые принципы

### 2.1 Интеграционные тесты
- Каждый тест проверяет **связку "генератор → валидатор"**.
- Все тесты следуют стандарту **ГОСТ-ВАЛИДАТОР-2025**.
- Основная цель — гарантировать:
  1. корректность структуры JSON;
  2. математическую валидность данных;
  3. покрытие всех паттернов в генераторе;
  4. стабильность при множественных запусках.

### 2.2 Формат тестов
- Файл теста должен называться:
test_task_<номер>_<тема>.py

markdown
Копировать код
Пример:  
`test_task_6_common_fractions.py`
- Расположение:
matunya_bot_final/tests/task_generators/task_<номер>/

markdown
Копировать код

### 2.3 Обязательные блоки теста
Каждый тест содержит:
1. **Импорт** генератора и валидатора.  
2. **Список REQUIRED_KEYS** — все обязательные поля JSON.  
3. **_assert_common_structure(task)** — проверка структуры.  
4. **Основной тест** `test_task_<n>_<topic>_pipeline()`:
 - создаёт выборку (100–200 заданий);
 - вызывает генератор;
 - запускает валидатор;
 - собирает статистику по паттернам;
 - сообщает о первых 5 ошибках, если есть.

### 2.4 Ключевые переменные теста
| Имя | Назначение |
|-----|-------------|
| `REQUIRED_KEYS` | Список обязательных полей задания |
| `ALLOWED_PATTERNS` | Список допустимых паттернов (`"1.1"`, `"1.2"`, …) |
| `ALLOWED_SUBTYPES` | Коды подтипов (`"cf_addition_subtraction"`, …) |
| `pattern_counts` | Счётчик появления паттернов |
| `failures` | Список ошибок при генерации или валидации |

### 2.5 Поведение при ошибках
Если есть ошибки:
- Выводится отчёт с максимум 5 первыми примерами:
Failed 3 tasks. Examples: #12 (6_cf_addition_subtraction_ab12cd): Неверная структура expression_tree

yaml
Копировать код
- Тест завершается `AssertionError`.

### 2.6 Стандарты покрытия
- Каждый тест должен подтвердить, что **все паттерны генератора** были созданы хотя бы 1 раз.
- Минимум: `assert pattern_counts[pattern] > 0`.

---

## 3. Пример шаблона
```python
from matunya_bot_final.task_generators.task_6.generators.common_fractions_generator import generate_common_fractions_tasks
from matunya_bot_final.task_generators.task_6.validators.common_fractions_validator import validate_common_fractions_task

def test_task_6_common_fractions_pipeline() -> None:
  tasks = generate_common_fractions_tasks(10)
  for task in tasks:
      is_valid, errors = validate_common_fractions_task(task)
      assert is_valid, f"Ошибки: {errors}"
4. Стиль отчётности
Все тесты должны использовать единый стиль логов в CI/CD:

css
Копировать код
✅ [PASS] 6_cf_addition_subtraction_8a31bc
❌ [FAIL] 6_complex_fraction_3d7c2f — Ошибка: Знаменатель равен нулю
5. Поддержка и версия
Версия стандарта: Matyunya-2026 / GOST-TEST-1.0

Ответственный: Капитан Оленька 🌿

Актуальность: обновляется при изменении структуры заданий или логики валидаторов.
